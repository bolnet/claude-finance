---
phase: 12-walkthrough-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_walkthrough_hedge_fund.py
  - tests/test_walkthrough_investment_banking.py
  - tests/test_walkthrough_fpa.py
  - tests/test_walkthrough_accounting.py
  - tests/test_walkthrough_private_equity.py
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Each of the 5 walkthroughs has a dedicated test file that passes pytest"
    - "Tests validate that each walkthrough references its expected MCP tools in allowed-tools frontmatter"
    - "Tests validate that each walkthrough has the correct number of phases and steps"
    - "Tests validate role-specific framing keywords are present in each walkthrough"
    - "pytest tests/test_walkthrough_*.py passes with 0 failures across all 6 test files (5 new + 1 existing)"
  artifacts:
    - path: "tests/test_walkthrough_hedge_fund.py"
      provides: "Hedge fund walkthrough structure and tool coverage tests"
      min_lines: 100
    - path: "tests/test_walkthrough_investment_banking.py"
      provides: "Investment banking walkthrough structure and tool coverage tests"
      min_lines: 100
    - path: "tests/test_walkthrough_fpa.py"
      provides: "FP&A walkthrough structure and tool coverage tests"
      min_lines: 100
    - path: "tests/test_walkthrough_accounting.py"
      provides: "Accounting walkthrough structure and tool coverage tests"
      min_lines: 100
    - path: "tests/test_walkthrough_private_equity.py"
      provides: "Private equity walkthrough structure and tool coverage tests"
      min_lines: 100
  key_links:
    - from: "tests/test_walkthrough_*.py"
      to: ".claude/commands/walkthrough-*.md"
      via: "Path resolution and file read"
      pattern: "COMMANDS_DIR.*walkthrough-"
---

<objective>
Create 5 test files that validate all 5 remaining walkthrough slash commands (hedge fund, investment banking, FP&A, accounting, private equity) for structure, phase completeness, MCP tool coverage, and role-specific framing.

Purpose: Ensure all walkthroughs are structurally sound and reference the correct MCP tools before shipping v1.2.
Output: 5 new test files in tests/, all passing pytest.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/test_walkthrough_equity_research.py

The equity research test file is the TEMPLATE. Follow its exact pattern:
- Same imports (pathlib, re)
- Same COMMANDS_DIR resolution
- Same read_walkthrough_file() helper (adapted per walkthrough filename)
- Same test structure: file exists, frontmatter fields, allowed-tools coverage, no argument-hint, step count, phase count, role-specific content assertions, disclaimer

<interfaces>
<!-- From the template test file: tests/test_walkthrough_equity_research.py -->
```python
COMMANDS_DIR = Path(__file__).parent.parent / ".claude" / "commands"

REQUIRED_MCP_TOOLS = [...]  # Per-walkthrough tool list

def read_walkthrough_file():
    """Read walkthrough-{name}.md and return (frontmatter, body)."""
    # Parses YAML frontmatter from --- delimiters
    # Returns (frontmatter_string, body_string)
```

<!-- Each test file needs these adapted per walkthrough: -->
# 1. REQUIRED_MCP_TOOLS - list of mcp__finance__* tools from that walkthrough's allowed-tools
# 2. read_walkthrough_file() - reading the correct .md filename
# 3. test_has_N_steps() - correct step count for that walkthrough
# 4. test_has_N_phases() - correct phase count for that walkthrough
# 5. Role-specific ticker lists OR framing keyword lists
# 6. Role-specific content assertions (tables, summaries, disclaimers)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 5 walkthrough test files and verify with pytest</name>
  <files>
    tests/test_walkthrough_hedge_fund.py
    tests/test_walkthrough_investment_banking.py
    tests/test_walkthrough_fpa.py
    tests/test_walkthrough_accounting.py
    tests/test_walkthrough_private_equity.py
  </files>
  <action>
Create 5 test files following the EXACT pattern from tests/test_walkthrough_equity_research.py. Each file must:

1. Use the same imports, COMMANDS_DIR, and read_walkthrough_file() pattern (adapted for the correct filename)
2. Define REQUIRED_MCP_TOOLS specific to that walkthrough's allowed-tools frontmatter
3. Include these standard tests (matching the template naming pattern):
   - test_walkthrough_file_exists
   - test_frontmatter_required_fields (description, allowed-tools, model)
   - test_allowed_tools_coverage (all MCP tools from REQUIRED_MCP_TOOLS present)
   - test_no_argument_hint
   - test_has_N_steps (correct count per walkthrough)
   - test_has_N_phases (correct count per walkthrough)
   - test_disclaimer ("not financial advice")
4. Include role-specific tests unique to each walkthrough

**Per-walkthrough specifications:**

**Hedge Fund** (walkthrough-hedge-fund.md):
- REQUIRED_MCP_TOOLS: mcp__finance__validate_environment, mcp__finance__analyze_stock (NOT used but check what IS in allowed-tools), mcp__finance__get_returns (NOT used), mcp__finance__get_volatility, mcp__finance__get_risk_metrics, mcp__finance__compare_tickers, mcp__finance__correlation_map
- Actually check the frontmatter: allowed-tools includes all 7 market tools. Set REQUIRED_MCP_TOOLS to: validate_environment, analyze_stock, get_returns, get_volatility, get_risk_metrics, compare_tickers, correlation_map
- Steps: 15 (Step 1 through Step 15)
- Phases: 5 (Phase 1 through Phase 5)
- Required tickers: NVDA, AMD, AVGO, AAPL, MSFT, GOOGL
- Role framing test: at least 3 of ["trading desk", "volatility", "pair", "systematic", "regime", "CIO"]
- Tool mapping test: body contains "Bloomberg" or "FactSet" and "time saved" or "time saving"

**Investment Banking** (walkthrough-investment-banking.md):
- REQUIRED_MCP_TOOLS: validate_environment, analyze_stock, get_returns, get_volatility, get_risk_metrics, compare_tickers, correlation_map
- Steps: 19 (Step 1 through Step 19)
- Phases: 5 (Phase 1 through Phase 5)
- Required tickers: MSFT, GOOGL, AMZN, CRM, ORCL
- Role framing test: at least 3 of ["pitch book", "comps", "deal", "MD", "exhibit", "valuation"]
- Tool mapping test: body contains "Bloomberg" or "CapIQ" and "time saved" or "time saving"

**FP&A** (walkthrough-fpa.md):
- REQUIRED_MCP_TOOLS: validate_environment, ingest_csv, liquidity_predictor, predict_liquidity
- Steps: 11 (Step 1 through Step 11)
- Phases: 4 (Phase 1 through Phase 4)
- Role framing test: at least 3 of ["budget", "forecast", "FP&A", "VP of Finance", "ERP", "variance"]
- Content test: body contains "sample_portfolio.csv"
- Tool mapping test: body contains "Hyperion" or "Excel" and "time saved" or "time saving"

**Accounting** (walkthrough-accounting.md):
- REQUIRED_MCP_TOOLS: validate_environment, ingest_csv, investor_classifier, classify_investor
- Steps: 11 (Step 1 through Step 11)
- Phases: 4 (Phase 1 through Phase 4)
- Role framing test: at least 3 of ["controller", "audit", "general ledger", "quarterly close", "misclassification", "ERP"]
- Content test: body contains "sample_portfolio.csv"
- Tool mapping test: body contains "SAP" or "Oracle" and "time saved" or "time saving"

**Private Equity** (walkthrough-private-equity.md):
- REQUIRED_MCP_TOOLS: validate_environment, ingest_csv, investor_classifier, classify_investor
- Steps: 12 (Step 1 through Step 12)
- Phases: 4 (Phase 1 through Phase 4)
- Role framing test: at least 3 of ["deal team", "investment committee", "IC", "due diligence", "portfolio", "Fund III"]
- Content test: body contains "sample_portfolio.csv"
- Tool mapping test: body contains "PitchBook" or "Preqin" and "time saved" or "time saving"

After creating all 5 files, run: `pytest tests/test_walkthrough_*.py -v`

If any test fails, read the walkthrough file to confirm the correct counts/content, fix the test, and re-run until all pass.
  </action>
  <verify>
    <automated>cd /Users/aarjay/Documents/machine_learning_skill && python -m pytest tests/test_walkthrough_*.py -v</automated>
  </verify>
  <done>All 5 new test files exist, follow the equity research template pattern, and pytest passes with 0 failures across all 6 walkthrough test files (5 new + 1 existing)</done>
</task>

</tasks>

<verification>
- `pytest tests/test_walkthrough_*.py -v` passes with 0 failures
- All 6 walkthrough test files (including existing equity research) run together without conflicts
- Each test file validates: file existence, frontmatter, MCP tools, step/phase counts, role framing, disclaimer
</verification>

<success_criteria>
- 5 new test files created in tests/
- All tests pass: `pytest tests/test_walkthrough_*.py` shows 0 failures
- Each walkthrough's MCP tool list matches its allowed-tools frontmatter
- Each walkthrough's step and phase counts are validated
- Role-specific framing keywords are asserted per walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/12-walkthrough-test-suite/12-01-SUMMARY.md`
</output>
