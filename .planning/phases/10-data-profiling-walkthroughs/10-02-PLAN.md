---
phase: 10-data-profiling-walkthroughs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/walkthrough-accounting.md
  - .claude/skills/finance/SKILL.md
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03]

must_haves:
  truths:
    - "User can run /walkthrough-accounting and receive a multi-phase accounting/controller scenario"
    - "Walkthrough profiles the bundled sample CSV as transaction data via ingest_csv"
    - "Walkthrough uses investor classifier for anomaly detection prep with audit framing"
    - "All steps auto-run without user input"
    - "Output uses accounting language: controller, audit trail, consolidation, anomaly detection, ERP, trial balance"
  artifacts:
    - path: ".claude/commands/walkthrough-accounting.md"
      provides: "Accounting walkthrough slash command"
      contains: "ingest_csv"
    - path: ".claude/skills/finance/SKILL.md"
      provides: "Accounting intent routing"
      contains: "walkthrough-accounting"
  key_links:
    - from: ".claude/skills/finance/SKILL.md"
      to: ".claude/commands/walkthrough-accounting.md"
      via: "intent routing row directs users to /walkthrough-accounting"
      pattern: "walkthrough-accounting"
    - from: ".claude/commands/walkthrough-accounting.md"
      to: "demo/sample_portfolio.csv"
      via: "bundled CSV path reference"
      pattern: "sample_portfolio.csv"
---

<objective>
Create the /walkthrough-accounting slash command -- a self-running, multi-phase scenario for controllers and accounting teams covering transaction data profiling via CSV ingestion, anomaly detection prep using the investor classifier as a segment-based outlier detector, and ERP consolidation patterns.

Purpose: Accounting professionals get a walkthrough that simulates real transaction data auditing and consolidation workflows using existing MCP tools with role-appropriate language (controller, audit trail, general ledger, consolidation, anomaly detection).

Output: walkthrough-accounting.md slash command file + SKILL.md routing update
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-market-analysis-walkthroughs/09-01-SUMMARY.md

Template to follow (exact pattern):
@.claude/commands/walkthrough-equity-research.md
@.claude/commands/walkthrough-hedge-fund.md

Bundled CSV (MUST use this file path):
@demo/sample_portfolio.csv

SKILL.md (for intent routing update):
@.claude/skills/finance/SKILL.md

<interfaces>
<!-- CSV columns available in demo/sample_portfolio.csv -->
credit_score, debt_ratio, region, liquidity_risk, age, income, risk_tolerance, product_preference, segment

<!-- MCP tools this walkthrough will use -->
validate_environment() -- pre-flight check
ingest_csv(csv_path, target_column?) -- data profiling / transaction review
investor_classifier(csv_path, target_column?) -- train classification model (segment-based)
classify_investor(age, income, risk_tolerance, product_preference) -- classify individual records

<!-- Existing allowed-tools format from other walkthroughs -->
allowed-tools: Bash(python3:*), Bash(pip:*), Write, Read, mcp__finance__ping, mcp__finance__validate_environment, mcp__finance__ingest_csv, mcp__finance__investor_classifier, mcp__finance__classify_investor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create walkthrough-accounting.md slash command</name>
  <files>.claude/commands/walkthrough-accounting.md</files>
  <action>
Create `.claude/commands/walkthrough-accounting.md` following the exact structural pattern of `walkthrough-equity-research.md` and `walkthrough-hedge-fund.md`.

**Frontmatter (YAML between --- delimiters):**
- description: "Accounting walkthrough -- transaction data profiling, segment-based anomaly detection, and ERP consolidation patterns"
- allowed-tools: Bash(python3:*), Bash(pip:*), Write, Read, mcp__finance__ping, mcp__finance__validate_environment, mcp__finance__ingest_csv, mcp__finance__investor_classifier, mcp__finance__classify_investor
- model: sonnet

**Scenario framing (opening blockquote):**
You are a Senior Controller at a regional financial services firm preparing for the quarterly close. The accounting team has exported a client portfolio dataset from the ERP system for consolidation review. Before signing off on the trial balance, you need to profile the transaction data for completeness, identify segment distributions to flag potential misclassifications (anomaly detection), and run a classifier to validate that client segments in the general ledger match expected patterns. The scenario uses the bundled `demo/sample_portfolio.csv`.

**Walkthrough Instructions section:** Copy the exact pattern from equity-research -- execute in order, show results, plain-English explanation, separator, proceed immediately, no user input between steps.

**Phase structure (4 phases, ~12-14 steps total):**

**Phase 1: Pre-Flight Check (1 step)**
- Step 1: Call `validate_environment` -- confirm packages ready. Frame: "Before the quarterly close, the controller's data audit workflow depends on pandas for ledger data manipulation, scikit-learn for segment classification, and matplotlib for variance visualization. All must be present before the audit begins."

**Phase 2: Transaction Data Profiling (3-4 steps)**
- Step 2: Call `ingest_csv` with csv_path="demo/sample_portfolio.csv" (no target_column). Frame as: "The first step in any quarterly close is profiling the ERP export. The controller needs to verify: How many records were exported? Are all expected columns present? Are there null values that indicate data feed failures? This is the data completeness check that external auditors will ask about." Show column listing, row count, basic stats. Interpret each column in accounting terms: credit_score = client creditworthiness for A/R provisioning, debt_ratio = leverage indicator for balance sheet risk, region = consolidation entity, segment = general ledger classification.
- Step 3: Call `ingest_csv` with csv_path="demo/sample_portfolio.csv", target_column="segment". Frame as: "For anomaly detection, the controller designates the 'segment' column as the classification target. The segment distribution reveals whether client records are properly classified in the general ledger -- an uneven distribution (e.g., 90% in one segment) suggests systematic misclassification that the audit committee will flag." Interpret the segment distribution: how many categories, is it balanced, what does imbalance suggest.
- Step 4: Pure analysis step (NO tool call). "ERP Data Audit Report" -- compile findings from Steps 2-3 into an audit-ready data quality report. Frame as what the controller would present to the audit committee: data completeness score, column integrity, segment distribution assessment, any red flags for the external auditor. Use a table format: Column | Data Type | Completeness | Audit Risk Level.

**Phase 3: Segment Classification & Anomaly Detection (3-4 steps)**
- Step 5: Call `investor_classifier` with csv_path="demo/sample_portfolio.csv". Frame as: "The controller trains a classification model on the ERP export to establish the expected segment pattern. This model learns what a 'correctly classified' record looks like -- any record that the model classifies differently from the ledger entry is a potential misclassification (anomaly) that requires investigation."
- Step 6: Pure analysis step (NO tool call). "Classification Accuracy as Audit Metric" -- interpret the model's accuracy and feature importances in controller language. Frame: "A model accuracy of X% means X% of records in the ledger match the expected classification pattern. The remaining (100-X)% are potential anomalies -- records where the actual segment assignment deviates from what the model predicts based on the client's financial profile. In audit terms, this is the misclassification rate."
- Step 7: Call `classify_investor` with age=55, income=180000, risk_tolerance=0.43, product_preference="equities". Frame as: "Test Case 1 -- Expected Conservative Profile. The controller tests a client profile that should clearly classify as conservative (high income, moderate risk tolerance, equities preference). If the model agrees, this validates the classifier. If it disagrees, the segment assignment in the ledger needs review."
- Step 8: Call `classify_investor` with age=28, income=63000, risk_tolerance=0.19, product_preference="equities". Frame as: "Test Case 2 -- Ambiguous Profile. This client has characteristics that could fall into multiple segments (young, lower income, low risk tolerance but equities preference). The controller uses ambiguous cases to stress-test the classifier -- records like this are most likely to be misclassified in the ledger."

**Phase 4: Consolidation & Audit Synthesis (3-4 steps)**
- Step 9: Call `classify_investor` with age=40, income=120000, risk_tolerance=0.65, product_preference="bonds". Frame as: "Test Case 3 -- Cross-Check Profile. Testing a profile where the product preference (bonds) may conflict with the risk tolerance (moderate-high). The controller looks for internal consistency -- if the model classifies this differently than the ledger, it suggests the segment rules need updating for the next consolidation cycle."
- Step 10: Pure analysis step (NO tool call). "Anomaly Detection Summary" -- compile classifications from Steps 7-9 into an anomaly detection results table: Test Case | Profile | Model Classification | Expected | Match/Mismatch. Calculate the anomaly rate. Frame: "In a full production run, the controller would run this classifier against the entire ledger export and flag all mismatches for manual review before certifying the quarterly close."
- Step 11: Pure analysis step (NO tool call). "Quarterly Close Recommendation" -- final synthesis. Summarize: (1) Data completeness from Phase 2, (2) Segment classification accuracy from Phase 3, (3) Anomaly detection findings from Phase 4. Include a "What a Controller Would Do Next" section covering: running the classifier against the full GL extract, generating a misclassification exception report, presenting findings to the audit committee, updating segment assignment rules in the ERP, and scheduling pre-close data quality checks for next quarter.

**Traditional Tool Mapping table:** Map ingest_csv to SAP ABAP data dictionary review / Oracle GL export validation, investor_classifier to manual segment cross-referencing in Excel / audit sampling, classify_investor to individual record spot-checks during substantive testing.

**Footer:** Disclaimer ("not financial advice"), pointer to `/finance` for general analysis.

**CRITICAL constraints:**
- MUST reference `demo/sample_portfolio.csv` as the CSV path (not ask user for data)
- MUST use accounting/controller language throughout: audit trail, general ledger, consolidation, trial balance, quarterly close, audit committee, misclassification, ERP, segment
- MUST NOT use equity research, trading desk, deal pitch, or FP&A forecasting language
- Frame the investor_classifier as an anomaly detection / misclassification detection tool (not an investment tool)
- Frame classify_investor as individual record validation (not investor profiling)
- Follow the exact markdown structure of walkthrough-equity-research.md (frontmatter, scenario intro, instructions, phases with steps, tool mapping table, footer)
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
import re

p = Path('.claude/commands/walkthrough-accounting.md')
assert p.exists(), 'File missing'
text = p.read_text()

# Frontmatter
assert 'allowed-tools:' in text
assert 'mcp__finance__ingest_csv' in text
assert 'mcp__finance__investor_classifier' in text
assert 'mcp__finance__classify_investor' in text
assert 'mcp__finance__validate_environment' in text

# CSV reference
assert 'sample_portfolio.csv' in text

# Accounting framing
lower = text.lower()
assert 'audit' in lower
assert 'consolidation' in lower or 'controller' in lower
assert 'anomaly' in lower or 'misclassification' in lower

# Phases
for n in range(1, 5):
    assert re.search(rf'(?i)phase\s+{n}\b', text), f'Missing Phase {n}'

# Steps (at least 10)
step_count = len(re.findall(r'(?i)###\s+step\s+\d+', text))
assert step_count >= 10, f'Only {step_count} steps, need >= 10'

# No user input required
assert 'not financial advice' in lower
print('All checks passed')
"
    </automated>
  </verify>
  <done>walkthrough-accounting.md exists with 4 phases, 10+ steps, uses ingest_csv/investor_classifier/classify_investor tools, references demo/sample_portfolio.csv, uses accounting/controller/audit language throughout</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md intent routing for accounting walkthrough</name>
  <files>.claude/skills/finance/SKILL.md</files>
  <action>
Update `.claude/skills/finance/SKILL.md` with two additions:

1. **Intent classification table** (after the walkthrough-fpa row if it exists, or after the investment-banking row): Add a new row:
```
| `walkthrough-accounting` | "accounting walkthrough", "controller scenario", "transaction profiling", "anomaly detection walkthrough", "quarterly close walkthrough", "audit data review" | Direct user to run `/walkthrough-accounting` command for a controller data profiling and anomaly detection scenario |
```

2. **Role Walkthroughs table** (after the fpa row if it exists, or after the investment-banking row): Add a new row:
```
| `/walkthrough-accounting` | Controller / accounting | Transaction data profiling, segment-based anomaly detection, and ERP consolidation review |
```

3. **Routing paragraph** (after the FPA routing paragraph if it exists, or after the IB routing paragraph): Add:
```
If a user asks for an "accounting walkthrough", "controller scenario", "transaction profiling", "anomaly detection walkthrough", "quarterly close", or "audit data review", direct them to type `/walkthrough-accounting`.
```

Do NOT modify any other content. These are additive-only changes. If Plan 10-01 has already added the FPA rows, place accounting rows after them. If not, place after the investment-banking rows.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
text = Path('.claude/skills/finance/SKILL.md').read_text()
assert 'walkthrough-accounting' in text, 'Missing walkthrough-accounting in SKILL.md'
assert 'controller' in text.lower() or 'anomaly detection' in text.lower(), 'Missing accounting framing in SKILL.md'
# Verify existing walkthroughs still present
assert 'walkthrough-hedge-fund' in text
assert 'walkthrough-investment-banking' in text
assert 'walkthrough-equity-research' in text
print('All checks passed')
"
    </automated>
  </verify>
  <done>SKILL.md routes accounting queries to /walkthrough-accounting, intent table has accounting row, Role Walkthroughs table has accounting row, existing walkthrough routing preserved</done>
</task>

</tasks>

<verification>
1. `.claude/commands/walkthrough-accounting.md` exists with valid frontmatter, 4 phases, 10+ steps
2. Walkthrough references `demo/sample_portfolio.csv` (bundled CSV)
3. MCP tools used: validate_environment, ingest_csv, investor_classifier, classify_investor
4. Accounting framing language throughout (controller, audit, consolidation, anomaly, ledger)
5. SKILL.md has accounting intent routing row and Role Walkthroughs entry
6. No existing walkthrough routing broken
</verification>

<success_criteria>
- /walkthrough-accounting is a valid Claude Code slash command
- Running it produces a 4-phase scenario covering transaction profiling, segment classification, anomaly detection, and consolidation synthesis
- All output uses accounting/controller professional language (not FP&A, equity research, or trading desk language)
- SKILL.md correctly routes accounting queries to the new command
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-profiling-walkthroughs/10-02-SUMMARY.md`
</output>
