---
phase: 10-data-profiling-walkthroughs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/walkthrough-fpa.md
  - .claude/skills/finance/SKILL.md
autonomous: true
requirements: [FPA-01, FPA-02, FPA-03]

must_haves:
  truths:
    - "User can run /walkthrough-fpa and receive a multi-phase FP&A scenario"
    - "Walkthrough profiles the bundled sample CSV with target column identification"
    - "Walkthrough runs liquidity predictor for forecasting prep with budget variance framing"
    - "All steps auto-run without user input"
    - "Output uses FP&A language: budget variance, forecasting, resource allocation, ERP export"
  artifacts:
    - path: ".claude/commands/walkthrough-fpa.md"
      provides: "FP&A walkthrough slash command"
      contains: "ingest_csv"
    - path: ".claude/skills/finance/SKILL.md"
      provides: "FP&A intent routing"
      contains: "walkthrough-fpa"
  key_links:
    - from: ".claude/skills/finance/SKILL.md"
      to: ".claude/commands/walkthrough-fpa.md"
      via: "intent routing row directs users to /walkthrough-fpa"
      pattern: "walkthrough-fpa"
    - from: ".claude/commands/walkthrough-fpa.md"
      to: "demo/sample_portfolio.csv"
      via: "bundled CSV path reference"
      pattern: "sample_portfolio.csv"
---

<objective>
Create the /walkthrough-fpa slash command -- a self-running, multi-phase scenario for FP&A analysts covering CSV data profiling, target column identification, ERP export cleanup, and ML forecasting prep using the liquidity predictor.

Purpose: FP&A professionals get a walkthrough that simulates real ERP data profiling and budget forecasting workflows using existing MCP tools with role-appropriate language (budget variance, resource allocation, forecasting prep).

Output: walkthrough-fpa.md slash command file + SKILL.md routing update
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-market-analysis-walkthroughs/09-01-SUMMARY.md

Template to follow (exact pattern):
@.claude/commands/walkthrough-equity-research.md
@.claude/commands/walkthrough-hedge-fund.md

Bundled CSV (MUST use this file path):
@demo/sample_portfolio.csv

SKILL.md (for intent routing update):
@.claude/skills/finance/SKILL.md

<interfaces>
<!-- CSV columns available in demo/sample_portfolio.csv -->
credit_score, debt_ratio, region, liquidity_risk, age, income, risk_tolerance, product_preference, segment

<!-- MCP tools this walkthrough will use -->
validate_environment() -- pre-flight check
ingest_csv(csv_path, target_column?) -- data profiling
liquidity_predictor(csv_path, target_column?) -- train regression model
predict_liquidity(credit_score, debt_ratio, region) -- score individual scenarios

<!-- Existing allowed-tools format from other walkthroughs -->
allowed-tools: Bash(python3:*), Bash(pip:*), Write, Read, mcp__finance__ping, mcp__finance__validate_environment, mcp__finance__ingest_csv, mcp__finance__liquidity_predictor, mcp__finance__predict_liquidity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create walkthrough-fpa.md slash command</name>
  <files>.claude/commands/walkthrough-fpa.md</files>
  <action>
Create `.claude/commands/walkthrough-fpa.md` following the exact structural pattern of `walkthrough-equity-research.md` and `walkthrough-hedge-fund.md`.

**Frontmatter (YAML between --- delimiters):**
- description: "FP&A walkthrough -- ERP data profiling, target column identification, and budget forecasting prep using liquidity predictor"
- allowed-tools: Bash(python3:*), Bash(pip:*), Write, Read, mcp__finance__ping, mcp__finance__validate_environment, mcp__finance__ingest_csv, mcp__finance__liquidity_predictor, mcp__finance__predict_liquidity
- model: sonnet

**Scenario framing (opening blockquote):**
You are a Senior FP&A Analyst at a mid-market company preparing quarterly budget variance analysis. Finance has exported a client portfolio dataset from the ERP system. Before building the forecast model, you need to profile the data quality, identify which columns drive liquidity risk (your forecasting target), clean up the ERP export anomalies, and run a regression model to generate budget projections. The scenario uses the bundled `demo/sample_portfolio.csv`.

**Walkthrough Instructions section:** Copy the exact pattern from equity-research -- execute in order, show results, plain-English explanation, separator, proceed immediately, no user input between steps.

**Phase structure (4 phases, ~12-14 steps total):**

**Phase 1: Pre-Flight Check (1 step)**
- Step 1: Call `validate_environment` -- confirm packages ready. Frame: "An FP&A forecasting workflow depends on pandas for data manipulation, scikit-learn for regression modeling, and matplotlib for variance visualization."

**Phase 2: ERP Data Profiling (3-4 steps)**
- Step 2: Call `ingest_csv` with csv_path="demo/sample_portfolio.csv" (no target_column). Frame as: "Before building any forecast, the FP&A team profiles the ERP export to understand data completeness, column types, and distribution. This is the 'data audit' step that prevents garbage-in-garbage-out forecasting." Show column listing, row count, basic stats. Interpret: which columns are numeric vs categorical, are there missing values, what looks like the target variable for forecasting.
- Step 3: Call `ingest_csv` with csv_path="demo/sample_portfolio.csv", target_column="liquidity_risk". Frame as: "Now that we've identified liquidity_risk as the forecasting target, we re-profile with the target column specified. This isolates the dependent variable and shows its distribution -- essential for understanding whether the target is balanced or skewed, which affects model selection." Interpret the target column distribution in FP&A terms: "Liquidity risk scores range from X to Y, with a mean of Z -- this tells us the portfolio's overall risk posture for budget planning."
- Step 4: Pure analysis step (NO tool call). "ERP Data Quality Assessment" -- compile findings from Steps 2-3 into a data quality report card: column completeness, data types, target variable suitability, any red flags. Frame in FP&A language: "Before proceeding to the forecasting model, the FP&A team would present this data quality summary to the VP of Finance to confirm the ERP export is fit for purpose."

**Phase 3: Forecasting Model Build (3-4 steps)**
- Step 5: Call `liquidity_predictor` with csv_path="demo/sample_portfolio.csv". Frame as: "The FP&A team builds a regression model to predict liquidity risk from the profiled ERP data. This model quantifies which financial factors (credit score, debt ratio, income) most strongly drive the forecasting target -- the same factors that explain budget variance quarter over quarter."
- Step 6: Pure analysis step (NO tool call). "Feature Importance for Budget Drivers" -- interpret the model's feature importances in FP&A terms. Map each top feature to a budget line item: "credit_score correlates to client creditworthiness (Accounts Receivable risk), debt_ratio maps to leverage exposure (Balance Sheet risk), income maps to revenue capacity (Revenue Forecast input)." Build a table: Feature | Budget Line Item | Impact on Forecast.
- Step 7: Call `predict_liquidity` with credit_score=750, debt_ratio=0.3, region="Northeast". Frame as: "Scenario 1 -- Strong Client Profile. The FP&A team tests the model with a high-credit, low-debt client to establish the baseline for best-case budget assumptions."
- Step 8: Call `predict_liquidity` with credit_score=520, debt_ratio=0.7, region="South". Frame as: "Scenario 2 -- Stressed Client Profile. Testing with a low-credit, high-debt client establishes the worst-case budget assumptions. The spread between Scenario 1 and Scenario 2 defines the variance range the budget model must accommodate."

**Phase 4: Budget Variance Synthesis (2-3 steps)**
- Step 9: Call `predict_liquidity` with credit_score=650, debt_ratio=0.45, region="Midwest". Frame as: "Scenario 3 -- Base Case. The median-profile client represents the most likely budget outcome. Comparing base case to best case and worst case gives the VP of Finance the three-scenario budget range."
- Step 10: Pure analysis step (NO tool call). "Three-Scenario Budget Summary" -- compile predictions from Steps 7-9 into a three-scenario table: Best Case | Base Case | Worst Case with liquidity risk scores. Calculate the variance spread. Frame: "This three-scenario framework is the standard FP&A deliverable for quarterly budget presentations. The VP of Finance uses the spread to set reserve requirements and resource allocation targets."
- Step 11: Pure analysis step (NO tool call). "Quarterly Budget Recommendation" -- final synthesis. Summarize: (1) Data quality assessment from Phase 2, (2) Key budget drivers from Phase 3, (3) Three-scenario range from Phase 4. Include a "What an FP&A Analyst Would Do Next" section covering: building the full quarterly forecast in Excel, presenting variance analysis to CFO, adjusting reserve ratios based on model spread, and scheduling monthly re-runs.

**Traditional Tool Mapping table:** Map ingest_csv to SAP/Oracle ERP export review, liquidity_predictor to Excel regression add-in or Hyperion planning model, predict_liquidity to manual what-if scenario tables.

**Footer:** Disclaimer ("not financial advice"), pointer to `/finance` for general analysis.

**CRITICAL constraints:**
- MUST reference `demo/sample_portfolio.csv` as the CSV path (not ask user for data)
- MUST use FP&A language throughout: budget variance, forecasting, resource allocation, ERP export, quarterly budget, VP of Finance, reserve requirements
- MUST NOT use equity research, trading desk, or deal pitch language
- Follow the exact markdown structure of walkthrough-equity-research.md (frontmatter, scenario intro, instructions, phases with steps, tool mapping table, footer)
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
import re

p = Path('.claude/commands/walkthrough-fpa.md')
assert p.exists(), 'File missing'
text = p.read_text()

# Frontmatter
assert 'allowed-tools:' in text
assert 'mcp__finance__ingest_csv' in text
assert 'mcp__finance__liquidity_predictor' in text
assert 'mcp__finance__predict_liquidity' in text
assert 'mcp__finance__validate_environment' in text

# CSV reference
assert 'sample_portfolio.csv' in text

# FPA framing
lower = text.lower()
assert 'budget' in lower
assert 'forecast' in lower
assert 'erp' in lower

# Phases
for n in range(1, 5):
    assert re.search(rf'(?i)phase\s+{n}\b', text), f'Missing Phase {n}'

# Steps (at least 10)
step_count = len(re.findall(r'(?i)###\s+step\s+\d+', text))
assert step_count >= 10, f'Only {step_count} steps, need >= 10'

# No user input required
assert 'not financial advice' in lower
print('All checks passed')
"
    </automated>
  </verify>
  <done>walkthrough-fpa.md exists with 4 phases, 10+ steps, uses ingest_csv/liquidity_predictor/predict_liquidity tools, references demo/sample_portfolio.csv, uses FP&A budget/forecasting language throughout</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md intent routing for FP&A walkthrough</name>
  <files>.claude/skills/finance/SKILL.md</files>
  <action>
Update `.claude/skills/finance/SKILL.md` with two additions:

1. **Intent classification table** (around line 33-34, after the investment-banking row): Add a new row:
```
| `walkthrough-fpa` | "fpa walkthrough", "budget variance analysis", "erp data profiling", "forecasting prep walkthrough", "fp&a scenario" | Direct user to run `/walkthrough-fpa` command for an FP&A data profiling and budget forecasting scenario |
```

2. **Role Walkthroughs table** (around line 48-50, after the investment-banking row): Add a new row:
```
| `/walkthrough-fpa` | FP&A analyst | ERP data profiling, target column identification, and three-scenario budget forecasting |
```

3. **Routing paragraph** (after the existing IB routing paragraph, around line 54): Add:
```
If a user asks for an "fpa walkthrough", "budget variance analysis", "erp data profiling", "forecasting prep", or "fp&a scenario", direct them to type `/walkthrough-fpa`.
```

Do NOT modify any other content. These are additive-only changes.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
text = Path('.claude/skills/finance/SKILL.md').read_text()
assert 'walkthrough-fpa' in text, 'Missing walkthrough-fpa in SKILL.md'
assert 'budget variance' in text.lower() or 'budget forecasting' in text.lower(), 'Missing FPA framing in SKILL.md'
# Verify existing walkthroughs still present
assert 'walkthrough-hedge-fund' in text
assert 'walkthrough-investment-banking' in text
assert 'walkthrough-equity-research' in text
print('All checks passed')
"
    </automated>
  </verify>
  <done>SKILL.md routes FP&A queries to /walkthrough-fpa, intent table has fpa row, Role Walkthroughs table has fpa row, existing walkthrough routing preserved</done>
</task>

</tasks>

<verification>
1. `.claude/commands/walkthrough-fpa.md` exists with valid frontmatter, 4 phases, 10+ steps
2. Walkthrough references `demo/sample_portfolio.csv` (bundled CSV)
3. MCP tools used: validate_environment, ingest_csv, liquidity_predictor, predict_liquidity
4. FP&A framing language throughout (budget, forecast, ERP, variance)
5. SKILL.md has fpa intent routing row and Role Walkthroughs entry
6. No existing walkthrough routing broken
</verification>

<success_criteria>
- /walkthrough-fpa is a valid Claude Code slash command
- Running it produces a 4-phase scenario covering data profiling, forecasting model build, and three-scenario budget synthesis
- All output uses FP&A professional language (not equity research or trading desk language)
- SKILL.md correctly routes FP&A queries to the new command
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-profiling-walkthroughs/10-01-SUMMARY.md`
</output>
