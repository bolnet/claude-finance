---
phase: 13-site-scaffolding-and-visual-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/.nojekyll
  - docs/index.html
  - docs/features.html
  - docs/walkthroughs.html
  - docs/getting-started.html
  - docs/assets/css/style.css
  - docs/assets/js/main.js
  - docs/assets/images/compare-tech-stocks.png
  - docs/assets/images/compare-semiconductor-stocks.png
  - docs/assets/images/correlation-heatmap.png
  - docs/assets/images/volatility-analysis.png
  - docs/assets/images/confusion-matrix.png
  - docs/assets/images/feature-importance.png
  - docs/assets/images/eda-credit-risk.png
  - docs/assets/images/residual-plot.png
  - docs/assets/images/favicon.png
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - VIS-01
  - VIS-02
  - VIS-03

must_haves:
  truths:
    - "docs/ folder contains .nojekyll as the first committed file"
    - "index.html loads locally in a browser with styled content and zero console errors"
    - "All 4 HTML pages share identical head/nav/footer template blocks"
    - "All href and src attributes use relative paths (no leading /)"
    - "Page is readable at 375px viewport width without horizontal scrolling"
    - "8 curated chart PNGs exist in docs/assets/images/ with stable descriptive filenames"
    - "Every image file in docs/assets/images/ is under 150KB"
    - "A favicon.png is present and referenced in the HTML head"
  artifacts:
    - path: "docs/.nojekyll"
      provides: "Disables Jekyll processing"
      contains: ""
    - path: "docs/index.html"
      provides: "Placeholder landing page with full template"
      min_lines: 30
    - path: "docs/assets/css/style.css"
      provides: "Shared stylesheet with mobile breakpoints"
      min_lines: 40
    - path: "docs/assets/js/main.js"
      provides: "Mobile nav toggle"
      min_lines: 8
    - path: "docs/assets/images/compare-tech-stocks.png"
      provides: "Hero comparison chart"
    - path: "docs/assets/images/favicon.png"
      provides: "Browser tab icon"
  key_links:
    - from: "docs/index.html"
      to: "docs/assets/css/style.css"
      via: "relative href in head"
      pattern: 'href="assets/css/style.css"'
    - from: "docs/index.html"
      to: "docs/assets/js/main.js"
      via: "relative src before closing body"
      pattern: 'src="assets/js/main.js"'
    - from: "docs/index.html"
      to: "docs/assets/images/favicon.png"
      via: "link rel icon in head"
      pattern: 'href="assets/images/favicon.png"'
    - from: "docs/index.html"
      to: "docs/features.html"
      via: "nav link"
      pattern: 'href="features.html"'
---

<objective>
Create the complete docs/ folder structure for GitHub Pages deployment: .nojekyll, 4 HTML pages (index + 3 stubs) sharing a single template with nav/footer, mobile-responsive CSS, nav toggle JS, 8 curated chart PNGs web-optimized under 150KB each, and a favicon.

Purpose: Every content page in Phases 14-15 will copy this template exactly. Getting the folder structure, relative paths, viewport meta, and image assets right NOW prevents rework later.

Output: Complete docs/ folder ready for GitHub Pages deployment — all infrastructure and visual asset requirements satisfied.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-site-scaffolding-and-visual-assets/13-RESEARCH.md

Source images for curation (verified sizes from research):
- finance_output/charts/compare_AAPL_GOOGL_MSFT_NVDA_2025-03-18.png (185KB - needs resize)
- finance_output/charts/compare_AMD_AVGO_INTC_NVDA_QCOM_2025-03-18.png (229KB - needs resize)
- finance_output/charts/correlation_AAPL_GOOGL_MSFT_2025-03-18.png (37KB - copy as-is)
- finance_output/charts/aapl_volatility_2025-03-18.png (68KB - copy as-is)
- finance_output/charts/confusion_matrix.png (35KB - copy as-is)
- finance_output/charts/feature_importance.png (50KB - copy as-is)
- finance_output/charts/eda_credit_score.png (19KB - copy as-is)
- finance_output/charts/residual_plot.png (57KB - copy as-is)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ folder structure with HTML template, CSS, and JS</name>
  <files>
    docs/.nojekyll
    docs/index.html
    docs/features.html
    docs/walkthroughs.html
    docs/getting-started.html
    docs/assets/css/style.css
    docs/assets/js/main.js
  </files>
  <action>
Create the complete docs/ folder structure. Order matters — .nojekyll FIRST.

1. Create empty `docs/.nojekyll` file (disables Jekyll processing).

2. Create `docs/assets/css/style.css` with the mobile-responsive baseline from the research:
   - box-sizing border-box reset
   - System font stack: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
   - Nav: flex layout, dark blue (#0f3460) background, white links
   - .nav-toggle hidden on desktop, visible at max-width: 640px
   - .nav-links: flex row on desktop, hidden column on mobile, .open class shows them
   - img max-width: 100% (never overflow container)
   - Hero section: centered text, padding, CTA button styled with accent color (#e94560)
   - Footer: centered text, muted color, border-top
   - Main content: max-width 1100px, margin 0 auto, padding for mobile
   - Add styles for .nav-brand (white, no underline, font-weight bold)
   - Add .nav-links a styles (white, no underline, hover underline)
   - Add .cta-primary styles (background #e94560, white text, padding, border-radius, no underline)

3. Create `docs/assets/js/main.js` (~12 lines):
   - querySelector for .nav-toggle and .nav-links
   - Toggle click handler adds/removes .open class on .nav-links
   - All nav link clicks remove .open class (close menu on navigation)

4. Create `docs/index.html` — the canonical template that Phases 14-15 will copy:
   - DOCTYPE html, lang="en"
   - Head: charset UTF-8, viewport meta (width=device-width, initial-scale=1), title "Finance AI Skill", meta description, link to favicon.png (relative: assets/images/favicon.png), link to style.css (relative: assets/css/style.css)
   - OG meta tags with placeholder values (og:image URL filled in Phase 15)
   - Header with nav: .nav-brand "Finance AI Skill" linking to index.html, .nav-toggle hamburger button, .nav-links ul with 4 li items (Home, Features, Walkthroughs, Get Started) — ALL relative hrefs
   - Main with placeholder hero section: h1 "Finance AI Skill", p "Professional-grade financial analysis in plain English. No Python required.", CTA link to getting-started.html
   - Add one sample image below hero: img src="assets/images/compare-tech-stocks.png" alt="Tech stock comparison chart" loading="lazy"
   - Footer: disclaimer text "For educational/informational purposes only. Not financial advice."
   - Script tag before closing body: src="assets/js/main.js" (relative)
   - CRITICAL: Every href and src must be relative (no leading /)

5. Create 3 stub pages (features.html, walkthroughs.html, getting-started.html):
   - Each is a COPY of index.html with the same head/nav/footer template
   - Only the main content differs: simple h1 with page name + "Coming soon" paragraph
   - Title tag updated per page: "Features | Finance AI Skill", etc.
   - Nav links identical across all 4 pages (same relative hrefs work because all pages are at docs/ root level)
   - CRITICAL: Same relative paths work for all pages since they are siblings in docs/

VALIDATION: After creating all files, verify:
- No href or src contains a leading / character
- All 4 HTML files have identical nav blocks
- viewport meta present in all 4 files
  </action>
  <verify>
    <automated>test -f docs/.nojekyll && test -f docs/index.html && test -f docs/features.html && test -f docs/walkthroughs.html && test -f docs/getting-started.html && test -f docs/assets/css/style.css && test -f docs/assets/js/main.js && echo "All 7 files exist" && grep -rL "viewport" docs/*.html | wc -l | grep -q "^0$" && echo "All pages have viewport meta" && grep -c 'href="/' docs/*.html 2>/dev/null | grep -v ':0$' | wc -l | grep -q "^0$" && echo "No root-absolute hrefs found" && grep -c 'src="/' docs/*.html 2>/dev/null | grep -v ':0$' | wc -l | grep -q "^0$" && echo "No root-absolute srcs found"</automated>
  </verify>
  <done>7 files created in docs/. All 4 HTML pages share identical head/nav/footer. All paths are relative. Viewport meta present in every page. .nojekyll exists.</done>
</task>

<task type="auto">
  <name>Task 2: Curate chart images and create favicon</name>
  <files>
    docs/assets/images/compare-tech-stocks.png
    docs/assets/images/compare-semiconductor-stocks.png
    docs/assets/images/correlation-heatmap.png
    docs/assets/images/volatility-analysis.png
    docs/assets/images/confusion-matrix.png
    docs/assets/images/feature-importance.png
    docs/assets/images/eda-credit-risk.png
    docs/assets/images/residual-plot.png
    docs/assets/images/favicon.png
  </files>
  <action>
Curate 8 chart PNGs from finance_output/charts/ and create a favicon. Physical copies required (GitHub Pages does not follow symlinks).

1. Create docs/assets/images/ directory if not already created by Task 1.

2. Resize and copy the 2 oversized charts using macOS `sips` (no install needed):
   ```
   sips -Z 800 finance_output/charts/compare_AAPL_GOOGL_MSFT_NVDA_2025-03-18.png --out docs/assets/images/compare-tech-stocks.png
   sips -Z 800 finance_output/charts/compare_AMD_AVGO_INTC_NVDA_QCOM_2025-03-18.png --out docs/assets/images/compare-semiconductor-stocks.png
   ```

3. Copy the 6 charts that are already under 150KB:
   ```
   cp finance_output/charts/correlation_AAPL_GOOGL_MSFT_2025-03-18.png docs/assets/images/correlation-heatmap.png
   cp finance_output/charts/aapl_volatility_2025-03-18.png docs/assets/images/volatility-analysis.png
   cp finance_output/charts/confusion_matrix.png docs/assets/images/confusion-matrix.png
   cp finance_output/charts/feature_importance.png docs/assets/images/feature-importance.png
   cp finance_output/charts/eda_credit_score.png docs/assets/images/eda-credit-risk.png
   cp finance_output/charts/residual_plot.png docs/assets/images/residual-plot.png
   ```

4. Create favicon.png — use Python Pillow to generate a simple 32x32 PNG:
   - Dark blue (#0f3460) background square
   - White text "FA" centered (use a basic font)
   - Save as docs/assets/images/favicon.png
   - If Pillow is not available, create a minimal 32x32 dark blue PNG using raw Python (struct + zlib for a valid PNG) or download from favicon.io with text "FA" background #0f3460

5. Verify ALL images are under 150KB:
   ```
   find docs/assets/images -name "*.png" -size +150k
   ```
   This command must return EMPTY (no files over 150KB). If any file exceeds 150KB after resize, run sips -Z 600 on it to further reduce.

NOTE: The source files in finance_output/charts/ may have been regenerated since research was done. If any source file listed above does not exist, check for similarly named files (the date portion may differ) using `ls finance_output/charts/compare_AAPL_GOOGL_MSFT_*.png` and use the most recent match.
  </action>
  <verify>
    <automated>ls docs/assets/images/*.png | wc -l | grep -q "9" && echo "9 PNG files present (8 charts + favicon)" && test -z "$(find docs/assets/images -name '*.png' -size +150k)" && echo "All images under 150KB" && test -f docs/assets/images/favicon.png && echo "Favicon exists"</automated>
  </verify>
  <done>8 curated chart PNGs with stable descriptive filenames exist in docs/assets/images/, all under 150KB. Favicon.png present. No date stamps or ticker-date combos in filenames.</done>
</task>

</tasks>

<verification>
Phase 13 infrastructure verification (run after both tasks):

1. File structure complete:
   ```
   ls -la docs/.nojekyll docs/index.html docs/features.html docs/walkthroughs.html docs/getting-started.html docs/assets/css/style.css docs/assets/js/main.js
   ls docs/assets/images/*.png | wc -l  # expect 9
   ```

2. No root-absolute paths in any HTML:
   ```
   grep -rn 'href="/' docs/*.html
   grep -rn 'src="/' docs/*.html
   ```
   Both must return empty.

3. All images under 150KB:
   ```
   find docs/assets/images -name "*.png" -size +150k
   ```
   Must return empty.

4. Viewport meta in all pages:
   ```
   grep -l "viewport" docs/*.html | wc -l  # expect 4
   ```

5. Existing Python tests still pass:
   ```
   pytest tests/ -q --tb=short -m "not network"
   ```
</verification>

<success_criteria>
- docs/ folder contains .nojekyll + 4 HTML pages + CSS + JS + 9 images (8 charts + favicon)
- All HTML pages share identical head/nav/footer blocks
- All href/src attributes use relative paths (no leading /)
- All images under 150KB with stable descriptive filenames
- Viewport meta present in all 4 pages
- Mobile nav toggle works (hamburger visible at 640px, toggles menu)
- Existing pytest suite passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/13-site-scaffolding-and-visual-assets/13-01-SUMMARY.md`
</output>
