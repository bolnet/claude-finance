---
phase: 17-portfolio-and-value-creation-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - finance-mcp-plugin/skills/private-equity/portfolio-monitoring/SKILL.md
  - finance-mcp-plugin/skills/private-equity/returns-analysis/SKILL.md
  - finance-mcp-plugin/skills/private-equity/unit-economics/SKILL.md
  - finance-mcp-plugin/skills/private-equity/value-creation-plan/SKILL.md
  - finance-mcp-plugin/skills/private-equity/ai-readiness/SKILL.md
autonomous: true
requirements: [SKILL-06, SKILL-07, SKILL-08, SKILL-09, SKILL-10]

must_haves:
  truths:
    - "PE professional can invoke /project:portfolio and receive a KPI tracking dashboard framework that calls classify_investor for classification drift and get_risk_metrics for market benchmarks"
    - "PE professional can invoke /project:returns and receive IRR/MOIC sensitivity tables backed by live get_returns and get_risk_metrics MCP data"
    - "PE professional can invoke /project:unit-economics and receive an ARR cohort and LTV/CAC analysis framework that uses ingest_csv for cohort data profiling"
    - "PE professional can invoke /project:value-creation and receive an EBITDA bridge with a 100-day plan and KPI target structure"
    - "PE professional can invoke /project:ai-readiness and receive a per-company go/wait gate with quick wins ranked by EBITDA impact"
    - "Each skill SKILL.md is 200-400 lines with clear PE frameworks"
  artifacts:
    - path: "finance-mcp-plugin/skills/private-equity/portfolio-monitoring/SKILL.md"
      provides: "Portfolio monitoring and KPI tracking skill"
      contains: "classify_investor"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/portfolio-monitoring/SKILL.md"
      provides: "Portfolio monitoring skill also references risk metrics"
      contains: "get_risk_metrics"
    - path: "finance-mcp-plugin/skills/private-equity/returns-analysis/SKILL.md"
      provides: "Returns analysis skill with IRR/MOIC"
      contains: "get_returns"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/returns-analysis/SKILL.md"
      provides: "Returns analysis skill also references risk metrics"
      contains: "get_risk_metrics"
    - path: "finance-mcp-plugin/skills/private-equity/unit-economics/SKILL.md"
      provides: "Unit economics skill with ARR/LTV/CAC"
      contains: "ingest_csv"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/value-creation-plan/SKILL.md"
      provides: "Value creation plan skill with EBITDA bridge"
      contains: "EBITDA"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/ai-readiness/SKILL.md"
      provides: "AI readiness assessment skill"
      contains: "EBITDA"
      min_lines: 200
  key_links:
    - from: "finance-mcp-plugin/skills/private-equity/portfolio-monitoring/SKILL.md"
      to: "MCP classify_investor tool"
      via: "skill references MCP tool for classification drift detection"
      pattern: "classify_investor"
    - from: "finance-mcp-plugin/skills/private-equity/portfolio-monitoring/SKILL.md"
      to: "MCP get_risk_metrics tool"
      via: "skill references MCP tool for market benchmarks"
      pattern: "get_risk_metrics"
    - from: "finance-mcp-plugin/skills/private-equity/returns-analysis/SKILL.md"
      to: "MCP get_returns tool"
      via: "skill references MCP tool for public comp return data"
      pattern: "get_returns"
    - from: "finance-mcp-plugin/skills/private-equity/unit-economics/SKILL.md"
      to: "MCP ingest_csv tool"
      via: "skill references MCP tool for cohort data profiling"
      pattern: "ingest_csv"
---

<objective>
Create 5 portfolio-stage PE skills (portfolio-monitoring, returns-analysis, unit-economics, value-creation-plan, ai-readiness) as SKILL.md files with MCP tool integration where applicable.

Purpose: PE professionals managing portfolio companies need domain-specific workflows for monitoring KPIs, analyzing returns, evaluating unit economics, planning value creation, and assessing AI readiness. Each skill provides a complete framework.
Output: 5 skill SKILL.md files (200-400 lines each) under finance-mcp-plugin/skills/private-equity/.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/16-plugin-infrastructure-and-deal-flow-skills/16-02-SUMMARY.md
</context>

<interfaces>
<!-- Existing SKILL.md pattern (established in Phase 16): -->
```yaml
---
name: [skill-name]
description: Use when a PE professional needs to [trigger phrases]...
version: 1.0.0
---
```

<!-- Each skill has: -->
<!-- 1. Role intro paragraph (2-3 sentences) -->
<!-- 2. Intent classification table (Intent | Trigger Phrases | Action) -->
<!-- 3. Workflow phases with numbered steps -->
<!-- 4. MCP tool integration sections (exact tool name + usage instructions) -->
<!-- 5. Output format specifications (tables, templates) -->
<!-- 6. Error handling / edge cases -->

<!-- Available MCP tools for these skills: -->
- `classify_investor` — ML investor classification scoring (used by portfolio-monitoring for drift detection)
- `get_risk_metrics` — Sharpe, drawdown, beta (used by portfolio-monitoring for benchmarks, returns-analysis for public comps)
- `get_returns` — Stock return analysis (used by returns-analysis for public comp return data)
- `ingest_csv` — CSV data profiling and exploration (used by unit-economics for cohort data)

<!-- Reference: deal-sourcing/SKILL.md is 328 lines, ic-memo is 394 lines — target similar density -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio-monitoring and returns-analysis skills</name>
  <files>
    finance-mcp-plugin/skills/private-equity/portfolio-monitoring/SKILL.md,
    finance-mcp-plugin/skills/private-equity/returns-analysis/SKILL.md
  </files>
  <action>
Create two SKILL.md files (200-400 lines each) following the established pattern from Phase 16 skills.

**1. portfolio-monitoring/SKILL.md (SKILL-06)**
- Frontmatter: name `portfolio-monitoring`, description mentioning KPI tracking, drift detection, quarterly dashboard, classification drift, market benchmarks
- Role intro: You are a PE portfolio monitoring specialist helping track KPI performance, detect classification drift, and generate quarterly dashboards
- Intent classification table:
  - `kpi-dashboard` — "KPI dashboard", "portfolio overview", "quarterly report", "how are our companies doing" — Generate KPI tracking dashboard with traffic-light status
  - `drift-detection` — "classification drift", "has the profile changed", "investor drift", "reclassify" — Run `classify_investor` to detect scoring drift vs. initial classification
  - `benchmark-compare` — "market benchmark", "how do we compare", "peer comparison", "industry metrics" — Call `get_risk_metrics` for public market benchmark comparison
  - `alert-review` — "red flags", "what needs attention", "alerts", "underperforming" — Analyze KPI trends for deterioration signals
  - `board-prep` — "board meeting", "board deck", "quarterly update", "LP report" — Generate board reporting package framework
- KPI framework: Revenue growth, EBITDA margin, net retention, cash conversion, customer count, employee headcount, LTV/CAC — each with target/actual/status (green/yellow/red) thresholds
- MCP integration: (1) `classify_investor` — re-run classification on current portfolio company data to detect drift from initial investment thesis scoring; include exact usage pattern with input description (2) `get_risk_metrics` — pull Sharpe, drawdown, beta for public market benchmarks to contextualize portfolio performance
- Quarterly dashboard template: Company-by-company KPI grid, portfolio-level aggregates, traffic-light summary, trend arrows, action items
- Board reporting framework: Executive summary, portfolio performance table, individual company highlights (top 3 performers, bottom 3), market context, upcoming milestones
- Output formats: KPI dashboard table, drift detection report, benchmark comparison table, board deck outline

**2. returns-analysis/SKILL.md (SKILL-07)**
- Frontmatter: name `returns-analysis`, description mentioning IRR/MOIC sensitivity, entry/exit scenarios, public comp returns
- Role intro: You are a PE returns analysis specialist helping model IRR/MOIC under various scenarios, run sensitivity analyses, and benchmark against public market comparables
- Intent classification table:
  - `irr-moic` — "IRR", "MOIC", "returns", "what's our return", "fund performance" — Calculate IRR/MOIC from entry terms and current/projected exit
  - `sensitivity` — "sensitivity analysis", "what if", "scenarios", "stress test", "upside/downside" — Generate IRR/MOIC sensitivity tables across entry multiple, exit multiple, and hold period
  - `public-comps` — "public comps", "comparable returns", "market returns", "how does the market compare" — Call `get_returns` and `get_risk_metrics` for public market comparable return data
  - `exit-modeling` — "exit scenarios", "exit multiple", "IPO vs trade sale", "secondary" — Model exit scenarios (IPO, strategic sale, secondary, dividend recap) with probability-weighted returns
  - `fund-attribution` — "fund attribution", "which deals drove returns", "winners and losers" — Attribute fund-level returns to individual deals
- IRR/MOIC framework: Entry enterprise value, equity invested, debt structure, projected exit EV, hold period, management fees/carry — compute gross and net IRR/MOIC
- Sensitivity table: 5x5 matrix varying entry multiple (rows) vs exit multiple (columns) showing IRR at each intersection; second table varying hold period vs exit multiple
- MCP integration: (1) `get_returns` — pull historical return data for public market comparables to benchmark PE returns against liquid alternatives (2) `get_risk_metrics` — pull Sharpe ratio, max drawdown for public comps to contextualize risk-adjusted returns
- Exit scenario modeling: IPO (discount to public comps), strategic sale (premium for synergies), secondary (discount for illiquidity), dividend recap (partial return, reduced equity at risk)
- Output formats: IRR/MOIC summary table, sensitivity matrices, public comp returns table, exit scenario comparison
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
skills_dir = Path('finance-mcp-plugin/skills/private-equity')
checks = {
    'portfolio-monitoring': ['classify_investor', 'get_risk_metrics'],
    'returns-analysis': ['get_returns', 'get_risk_metrics'],
}
for skill, refs in checks.items():
    f = skills_dir / skill / 'SKILL.md'
    assert f.exists(), f'Missing: {f}'
    text = f.read_text()
    lines = text.strip().split('\n')
    assert len(lines) >= 200, f'{skill} too short: {len(lines)} lines'
    assert len(lines) <= 400, f'{skill} too long: {len(lines)} lines'
    assert text.startswith('---'), f'{skill} missing YAML frontmatter'
    for ref in refs:
        assert ref in text, f'{skill} missing MCP reference: {ref}'
print('portfolio-monitoring and returns-analysis validated')
"
    </automated>
  </verify>
  <done>portfolio-monitoring/SKILL.md (200-400 lines) references classify_investor and get_risk_metrics. returns-analysis/SKILL.md (200-400 lines) references get_returns and get_risk_metrics. Both have YAML frontmatter and intent classification tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit-economics, value-creation-plan, and ai-readiness skills</name>
  <files>
    finance-mcp-plugin/skills/private-equity/unit-economics/SKILL.md,
    finance-mcp-plugin/skills/private-equity/value-creation-plan/SKILL.md,
    finance-mcp-plugin/skills/private-equity/ai-readiness/SKILL.md
  </files>
  <action>
Create three SKILL.md files (200-400 lines each) following the established pattern.

**3. unit-economics/SKILL.md (SKILL-08)**
- Frontmatter: name `unit-economics`, description mentioning ARR cohorts, LTV/CAC, net retention, revenue quality analysis, cohort data profiling
- Role intro: You are a PE unit economics specialist helping analyze revenue quality through cohort analysis, LTV/CAC ratios, net retention, and SaaS metrics
- Intent classification table:
  - `arr-cohort` — "ARR cohort", "cohort analysis", "vintage analysis", "retention by cohort", "when did they sign up" — Build ARR cohort waterfall from subscription data
  - `ltv-cac` — "LTV/CAC", "customer economics", "payback period", "acquisition cost", "lifetime value" — Calculate LTV/CAC ratio with payback period analysis
  - `net-retention` — "net retention", "NRR", "NDR", "expansion revenue", "churn", "upsell" — Compute net dollar retention with expansion/contraction/churn breakdown
  - `revenue-quality` — "revenue quality", "recurring vs one-time", "ARR vs MRR", "contract length", "concentration" — Assess revenue quality (recurring %, concentration, contract duration, logo vs dollar churn)
  - `cohort-profile` — "profile this data", "explore cohort data", "what's in this CSV", "data quality" — Call MCP `ingest_csv` to profile and summarize cohort data before analysis
- ARR cohort framework: Monthly/quarterly cohort buckets, starting ARR, expansion, contraction, churn, ending ARR per cohort — waterfall table format
- LTV/CAC model: Average revenue per account, gross margin, churn rate, discount rate for LTV; blended CAC (sales + marketing / new logos); payback period in months; LTV/CAC ratio with PE benchmark thresholds (>3x good, >5x excellent)
- Net retention breakdown: Beginning period ARR, expansion (upsell + cross-sell + price increase), contraction (downgrades), churn (cancellations), ending ARR — compute NRR%
- MCP integration: `ingest_csv` — profile cohort CSV exports to assess data quality, column completeness, date formats, and summary statistics before running cohort analysis
- Revenue quality scorecard: Recurring revenue %, top-10 customer concentration, average contract length, logo churn vs dollar churn, payment terms
- Output formats: Cohort waterfall table, LTV/CAC summary, NRR trend chart spec, revenue quality scorecard

**4. value-creation-plan/SKILL.md (SKILL-09)**
- Frontmatter: name `value-creation-plan`, description mentioning EBITDA bridge, 100-day plan, KPI targets, operational levers
- Role intro: You are a PE value creation specialist helping build post-acquisition plans including EBITDA bridges, 100-day plans, KPI targets, and operational improvement initiatives
- Intent classification table:
  - `ebitda-bridge` — "EBITDA bridge", "bridge to target", "value creation bridge", "how do we get to $X EBITDA" — Build EBITDA bridge from current to target with initiative-level detail
  - `hundred-day` — "100-day plan", "first 100 days", "post-close plan", "integration plan" — Generate structured 100-day post-acquisition plan
  - `kpi-targets` — "KPI targets", "set targets", "operating metrics", "what should we target" — Define KPI targets by function with milestones
  - `lever-analysis` — "operational levers", "value levers", "where can we improve", "low-hanging fruit" — Identify and size revenue and cost improvement levers
  - `initiative-track` — "track initiatives", "progress update", "are we on track", "initiative status" — Track value creation initiative progress vs plan
- EBITDA bridge framework: Current EBITDA -> Revenue growth initiatives (organic growth, pricing, new products, geographic expansion) -> Gross margin improvements (COGS reduction, procurement, mix shift) -> OpEx optimization (headcount, facilities, technology, outsourcing) -> One-time items (restructuring, integration costs) -> Target EBITDA. Each initiative: description, owner, timeline, EBITDA impact ($), confidence (high/medium/low), status
- 100-day plan template: Phase 1 Days 1-30 (assessment and quick wins), Phase 2 Days 31-60 (initiative launch), Phase 3 Days 61-100 (execution and early results). Each phase: key activities, deliverables, decision points, risks
- KPI target framework: Revenue KPIs (growth rate, ARR, ACV), Profitability KPIs (gross margin, EBITDA margin, cash conversion), Operational KPIs (NPS, employee retention, pipeline coverage), by quarter for first 12 months
- No MCP tool integration for this skill (pure framework/template skill)
- Output formats: EBITDA bridge waterfall table, 100-day plan Gantt-style outline, KPI target dashboard, initiative tracker table

**5. ai-readiness/SKILL.md (SKILL-10)**
- Frontmatter: name `ai-readiness`, description mentioning portfolio AI opportunity scan, per-company go/wait gate, quick wins ranked by EBITDA impact
- Role intro: You are a PE AI readiness assessment specialist helping evaluate which portfolio companies should adopt AI initiatives, identifying quick wins ranked by EBITDA impact
- Intent classification table:
  - `portfolio-scan` — "AI readiness scan", "which companies are ready", "portfolio AI assessment", "where should we invest in AI" — Scan portfolio for AI readiness across standard dimensions
  - `company-assess` — "assess this company", "is [company] ready for AI", "AI maturity", "readiness score" — Deep-dive single company AI readiness assessment with go/wait gate
  - `quick-wins` — "quick wins", "low-hanging fruit", "easy AI wins", "what can we do now" — Identify and rank AI quick wins by EBITDA impact and implementation difficulty
  - `roadmap-build` — "AI roadmap", "implementation plan", "phased rollout", "timeline for AI" — Build phased AI implementation roadmap for a portfolio company
  - `risk-assess` — "AI risks", "what could go wrong", "adoption barriers", "change management" — Assess AI adoption risks and mitigation strategies
- AI readiness dimensions: Data infrastructure (quality, accessibility, governance), Technical capability (engineering talent, cloud maturity, API readiness), Process maturity (documentation, standardization, automation baseline), Organization (leadership buy-in, change readiness, skills gap), Use case clarity (identified use cases, expected ROI, competitive pressure)
- Scoring: Each dimension scored 1-5, weighted by industry relevance. Aggregate score determines gate: GO (score >= 3.5, proceed with quick wins), WAIT (score 2.0-3.4, address gaps first), NOT READY (score < 2.0, foundational work needed)
- Quick wins framework: Ranked by (EBITDA impact * confidence) / implementation effort. Categories: Customer-facing AI (chatbots, personalization, churn prediction), Operations AI (demand forecasting, inventory optimization, predictive maintenance), Back-office AI (AP/AR automation, contract analysis, reporting automation). Each quick win: description, EBITDA impact estimate, implementation effort (weeks), confidence level, dependencies
- Go/wait gate: Per-company decision matrix with dimension scores, aggregate score, gate recommendation, top 3 quick wins, estimated total EBITDA uplift
- No MCP tool integration for this skill (pure assessment framework)
- Output formats: Portfolio readiness heatmap, company assessment scorecard, quick wins ranked table, implementation roadmap timeline
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
skills_dir = Path('finance-mcp-plugin/skills/private-equity')
checks = {
    'unit-economics': ['ingest_csv'],
    'value-creation-plan': ['EBITDA'],
    'ai-readiness': ['EBITDA'],
}
for skill, refs in checks.items():
    f = skills_dir / skill / 'SKILL.md'
    assert f.exists(), f'Missing: {f}'
    text = f.read_text()
    lines = text.strip().split('\n')
    assert len(lines) >= 200, f'{skill} too short: {len(lines)} lines'
    assert len(lines) <= 400, f'{skill} too long: {len(lines)} lines'
    assert text.startswith('---'), f'{skill} missing YAML frontmatter'
    for ref in refs:
        assert ref in text, f'{skill} missing reference: {ref}'
print('unit-economics, value-creation-plan, and ai-readiness validated')
"
    </automated>
  </verify>
  <done>unit-economics/SKILL.md references ingest_csv. value-creation-plan/SKILL.md contains EBITDA bridge and 100-day plan frameworks. ai-readiness/SKILL.md contains go/wait gate and quick wins ranked by EBITDA impact. All 200-400 lines with YAML frontmatter.</done>
</task>

</tasks>

<verification>
All portfolio/value-creation skill requirements verified:
- SKILL-06: portfolio-monitoring skill with classify_investor and get_risk_metrics references
- SKILL-07: returns-analysis skill with get_returns and get_risk_metrics references
- SKILL-08: unit-economics skill with ingest_csv reference
- SKILL-09: value-creation-plan skill with EBITDA bridge and 100-day plan
- SKILL-10: ai-readiness skill with go/wait gate and EBITDA-ranked quick wins
</verification>

<success_criteria>
1. All 5 SKILL.md files exist at finance-mcp-plugin/skills/private-equity/{name}/SKILL.md
2. Each skill is 200-400 lines with proper YAML frontmatter (name, description, version)
3. portfolio-monitoring references both classify_investor and get_risk_metrics
4. returns-analysis references both get_returns and get_risk_metrics
5. unit-economics references ingest_csv
6. value-creation-plan contains EBITDA bridge framework and 100-day plan
7. ai-readiness contains go/wait gate scoring and EBITDA-ranked quick wins
</success_criteria>

<output>
After completion, create `.planning/phases/17-portfolio-and-value-creation-skills/17-01-SUMMARY.md`
</output>
