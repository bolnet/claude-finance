---
phase: 15-getting-started-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started.html
  - docs/assets/css/style.css
autonomous: true
requirements:
  - START-01
  - START-02

must_haves:
  truths:
    - "Getting Started page presents a Claude Code install path with 4 numbered steps and copy-pasteable commands"
    - "Getting Started page presents a claude.ai install path with 5 numbered steps including ngrok setup"
    - "A callout warns that free-tier ngrok URL changes on restart"
    - "All install step language is plain English — no unexplained jargon (MCP, stdio, transport)"
    - "A 'First Analysis' section shows a sample prompt that works for both paths"
    - "A troubleshooting section pre-empts yfinance rate limits and ngrok auth issues"
  artifacts:
    - path: "docs/getting-started.html"
      provides: "Full two-path install page replacing page-stub"
      contains: "install-path"
    - path: "docs/assets/css/style.css"
      provides: "Install step layout CSS classes"
      contains: ".install-path"
  key_links:
    - from: "docs/getting-started.html"
      to: "docs/assets/css/style.css"
      via: "CSS classes: install-path, step, step-num, code-block, callout-note"
      pattern: "class=\"install-path\""
---

<objective>
Replace the getting-started.html stub with a full two-path installation page (Claude Code via stdio, claude.ai via HTTP + ngrok) with copy-pasteable commands and plain-English step descriptions. Add the required CSS classes to the shared style.css.

Purpose: This is the terminal conversion page — every CTA on the site lands here. Finance professionals must be able to follow either install path without developer knowledge.
Output: Complete getting-started.html with two install sections, troubleshooting, and first-analysis prompt. Extended style.css with install step layout classes.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-getting-started-and-polish/15-RESEARCH.md

@docs/getting-started.html
@docs/assets/css/style.css

<interfaces>
<!-- Confirmed install paths from research -->

Claude Code install (stdio):
  1. git clone https://github.com/bolnet/Claude-Finance.git && cd Claude-Finance
  2. pip install -e .
  3. Run claude from the Claude-Finance directory (.mcp.json auto-discovered)
  4. Type /finance to verify

claude.ai install (HTTP + ngrok):
  1. Clone + install (same as above)
  2. python -m finance_mcp.server_http (port 8000, endpoint http://0.0.0.0:8000/mcp)
  3. brew install ngrok && ngrok config add-authtoken YOUR_TOKEN && ngrok http 8000
  4. claude.ai Settings > Integrations > Add integration > paste ngrok URL
  5. Type /finance to verify

From pyproject.toml:
  - Package name: finance-mcp
  - Install: pip install -e . (from repo root, editable mode)
  - Dev extras: pip install -e ".[dev]" (not needed for end users)
  - HTTP entry point: finance-mcp-http (or python -m finance_mcp.server_http)

From finance-mcp-plugin/.mcp.json:
  { "mcpServers": { "finance": { "command": "python", "args": ["-m", "finance_mcp.server"] } } }

Existing CSS classes to reuse (from style.css):
  - .feature-category h2 — colored left border heading pattern (model for .path-heading)
  - .hero, .page-header — existing page section patterns
  - Body font: system-ui stack; brand dark blue: #0f3460; accent red: #e94560
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install step CSS classes to shared style.css</name>
  <files>docs/assets/css/style.css</files>
  <action>
Append the following CSS classes to the end of docs/assets/css/style.css (before any closing comment, after existing rules). These are the Getting Started page layout classes:

1. `.install-path` — section wrapper with bottom border separator and margin
2. `.install-path:last-of-type` — remove bottom border on last section
3. `.path-heading` — 1.5rem bold heading in #0f3460, with 3px solid #e94560 bottom border (matches existing feature-category h2 style)
4. `.path-audience` — 0.95rem grey subtitle below path heading
5. `.step` — flex row with 1.25rem gap, flex-start alignment
6. `.step-num` — 2rem circle, #0f3460 background, white bold number, centered
7. `.step-content` — flex:1 container for step text
8. `.step-content strong` — block display step title
9. `.step-content p` — grey description text
10. `.code-block` — dark (#1a1a2e) monospace block, 0.85rem, overflow-x:auto, 0.75rem padding
11. `.callout-note` — yellow (#fff8e1) background, 4px solid #f59e0b left border, rounded right corners
12. `.callout-note strong` — dark text for emphasis in callout
13. `.first-analysis` — section styling for the shared example prompt (use existing section padding pattern)

Use the exact CSS values from the research document code examples. Add a `/* ===== GETTING STARTED PAGE ===== */` section comment before the new rules.

Add responsive rules inside the existing `@media (max-width: 768px)` block:
- `.step` — change to flex-direction: column on mobile
- `.code-block` — reduce font-size to 0.78rem on mobile
  </action>
  <verify>
    <automated>grep -c "install-path\|step-num\|code-block\|callout-note\|path-heading" docs/assets/css/style.css</automated>
  </verify>
  <done>style.css contains all 13 new class definitions plus mobile responsive overrides. Count of new class selectors is >= 10.</done>
</task>

<task type="auto">
  <name>Task 2: Replace getting-started.html stub with full two-path install content</name>
  <files>docs/getting-started.html</files>
  <action>
Replace the `<main>` section of docs/getting-started.html (currently a page-stub) with full content. Keep the existing head, nav, and footer EXACTLY as-is (they are correct from Phase 13 template).

Page structure inside `<main>`:

1. **Page header section** (class="page-header"):
   - h1: "Get Started"
   - Subtitle: "Start analyzing finance data in minutes — no Python expertise required."

2. **Path A: Claude Code section** (class="install-path" id="claude-code"):
   - h2 (class="path-heading"): "Path A: Claude Code (recommended)"
   - p (class="path-audience"): "Best if you use Claude from the terminal or your code editor"
   - Step 1: "Clone the repository" — code-block with `git clone https://github.com/bolnet/Claude-Finance.git` and `cd Claude-Finance`
   - Step 2: "Install the finance tools" — plain English: "This installs everything the finance server needs — chart libraries, data feeds, and machine-learning models." Code-block: `pip install -e .`
   - Step 3: "Open Claude Code" — plain English: "Claude Code automatically finds the finance server when you start it from the project folder. No configuration file to edit." Code-block: `claude`
   - Step 4: "Verify it works" — plain English: "Type the command below. You should see a confirmation that the finance skill is active." Code-block: `/finance`

3. **Path B: claude.ai section** (class="install-path" id="claude-ai"):
   - h2 (class="path-heading"): "Path B: claude.ai (browser)"
   - p (class="path-audience"): "Best if you use Claude in the browser at claude.ai"
   - Step 1: "Clone and install" — same clone + pip install commands as Path A (repeat so user does not need to scroll up)
   - Step 2: "Start the finance server" — plain English: "This starts a local web server that claude.ai can connect to." Code-block: `python -m finance_mcp.server_http`
   - Plain text note: "Leave this terminal window open — the server must stay running."
   - Step 3: "Create a public tunnel with ngrok" — plain English: "ngrok makes your local server reachable from the internet so claude.ai can connect." Three sub-commands in a code-block: `brew install ngrok` (macOS, or link to ngrok.com/download), `ngrok config add-authtoken YOUR_TOKEN` (free signup at ngrok.com), `ngrok http 8000`. Plain text: "ngrok will show a public URL like https://abc123.ngrok-free.app — copy it."
   - **Callout note** (class="callout-note"): "Heads up: the ngrok URL changes every time you restart ngrok. You will need to update the URL in claude.ai Settings each time. To avoid this, claim a free static domain at dashboard.ngrok.com/domains (one per account)."
   - Step 4: "Add the server to claude.ai" — plain English: "Go to claude.ai, open Settings, then Integrations, click Add Integration, and paste the ngrok URL you copied."
   - Step 5: "Verify it works" — plain English: "Start a new conversation and type the command below." Code-block: `/finance`

4. **First Analysis section** (class="first-analysis"):
   - h2: "Your First Analysis"
   - p: "Once you see the finance skill is active, try this prompt:"
   - code-block with: `Show me a 6-month price comparison of Apple, Google, and Microsoft`
   - p: "Claude will pull live market data, generate a chart, and explain the trends — all in plain English."

5. **Troubleshooting section**:
   - h2: "Troubleshooting"
   - Three items as simple strong/p pairs:
     a. "Charts fail with a network error" — "Yahoo Finance limits how fast you can pull data. Wait 30 seconds and try again."
     b. "'python' not found" — "On macOS, try python3 instead of python in every command above."
     c. "ngrok shows 'authentication required'" — "Sign up for a free account at ngrok.com, then run `ngrok config add-authtoken YOUR_TOKEN` with the token from your ngrok dashboard."

CRITICAL RULES:
- ALL asset paths must be relative (assets/..., never /assets/...)
- Do NOT modify the `<head>`, `<header>`, or `<footer>` sections — they are correct
- Every `<pre class="code-block"><code>` block must contain the exact commands verified above
- No developer jargon without immediate plain-English explanation
- Use step-num spans (1, 2, 3, 4) inside .step divs as shown in research HTML pattern
  </action>
  <verify>
    <automated>python3 -c "
import re
html = open('docs/getting-started.html').read()
checks = [
    ('claude-code section', 'id=\"claude-code\"' in html),
    ('claude-ai section', 'id=\"claude-ai\"' in html),
    ('pip install', 'pip install -e .' in html),
    ('ngrok http', 'ngrok http 8000' in html),
    ('callout-note', 'callout-note' in html),
    ('code-block count >= 5', len(re.findall('code-block', html)) >= 5),
    ('no absolute paths', '/assets/' not in html),
    ('no page-stub', 'page-stub' not in html),
    ('step-num elements', len(re.findall('step-num', html)) >= 8),
]
for name, ok in checks:
    print(f'  {\"PASS\" if ok else \"FAIL\"}: {name}')
assert all(ok for _, ok in checks), 'Some checks failed'
print('All checks passed')
"
    </automated>
  </verify>
  <done>getting-started.html has two complete install paths (Claude Code 4 steps, claude.ai 5 steps), ngrok callout warning, first analysis example, troubleshooting section. Zero page-stub content remains. No absolute asset paths.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/ -x -q` — existing Python tests still pass (no regression)
2. Open docs/getting-started.html in a browser — both install paths display with numbered steps
3. All code blocks contain exact, verified commands
4. No absolute asset paths in the file
</verification>

<success_criteria>
- getting-started.html contains two visually separated install paths with numbered steps and copy-pasteable commands
- style.css contains all install step CSS classes
- Plain English throughout — no unexplained developer jargon
- Existing Python test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-getting-started-and-polish/15-01-SUMMARY.md`
</output>
