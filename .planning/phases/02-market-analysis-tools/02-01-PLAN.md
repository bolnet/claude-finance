---
phase: 02-market-analysis-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_market_tools.py
  - src/finance_mcp/tools/__init__.py
  - src/finance_mcp/tools/price_chart.py
  - src/finance_mcp/server.py
autonomous: true
requirements:
  - MKTX-01
  - MKTX-07

must_haves:
  truths:
    - "test_market_tools.py exists with 15 named test stubs that fail (not skip)"
    - "tools/ is a valid Python package (importable)"
    - "analyze_stock tool is registered in server.py and callable via FastMCP"
    - "analyze_stock returns a formatted string beginning with plain-English text"
    - "analyze_stock saves a PNG chart to finance_output/charts/"
  artifacts:
    - path: "tests/test_market_tools.py"
      provides: "All 15 test stubs for MKTX-01 through MKTX-07"
      exports: []
    - path: "src/finance_mcp/tools/__init__.py"
      provides: "Package marker enabling 'from finance_mcp.tools.x import y'"
      exports: []
    - path: "src/finance_mcp/tools/price_chart.py"
      provides: "analyze_stock MCP tool function"
      exports: ["analyze_stock"]
    - path: "src/finance_mcp/server.py"
      provides: "analyze_stock registered via mcp.add_tool()"
      contains: "mcp.add_tool(analyze_stock)"
  key_links:
    - from: "src/finance_mcp/tools/price_chart.py"
      to: "src/finance_mcp/adapter.py"
      via: "from finance_mcp.adapter import fetch_price_history, get_adjusted_prices, DataFetchError"
      pattern: "fetch_price_history"
    - from: "src/finance_mcp/tools/price_chart.py"
      to: "src/finance_mcp/output.py"
      via: "from finance_mcp.output import save_chart, format_output"
      pattern: "save_chart"
    - from: "src/finance_mcp/server.py"
      to: "src/finance_mcp/tools/price_chart.py"
      via: "mcp.add_tool(analyze_stock)"
      pattern: "add_tool"
---

<objective>
Wave 0 gate: create the test scaffold and the first MCP tool before any downstream plans run.

Purpose: Downstream plans (02-02, 02-03) depend on test stubs existing so their implementations can drive TDD cycles. server.py must import from tools/ before any tool module is built, establishing the registration pattern.
Output: tests/test_market_tools.py (15 stubs), src/finance_mcp/tools/__init__.py, src/finance_mcp/tools/price_chart.py (analyze_stock), server.py updated.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-analysis-tools/02-RESEARCH.md
@.planning/phases/02-market-analysis-tools/02-VALIDATION.md
@src/finance_mcp/adapter.py
@src/finance_mcp/output.py
@src/finance_mcp/server.py
</context>

<interfaces>
<!-- Key contracts for the executor. Load these; no codebase exploration needed. -->

From src/finance_mcp/adapter.py:
```python
class DataFetchError(Exception): ...

def fetch_price_history(ticker: str, start: str, end: Optional[str] = None, period: Optional[str] = None) -> pd.DataFrame:
    """Returns DataFrame with DatetimeIndex; 'Close' is adjusted price."""

def get_adjusted_prices(df: pd.DataFrame) -> pd.Series:
    """Returns df['Close'] — the split-and-dividend-adjusted price series."""

def fetch_multi_ticker(tickers: list[str], start: str, end: Optional[str] = None, period: Optional[str] = None) -> dict[str, pd.Series]:
    """Returns dict[ticker -> price Series]. Failures are logged and omitted."""
```

From src/finance_mcp/output.py:
```python
DISCLAIMER: str  # "For educational/informational purposes only. Not financial advice. ..."
CHART_DIR: str   # "finance_output/charts"

def save_chart(fig: plt.Figure, filename: str) -> str:
    """Saves fig to finance_output/charts/{filename}, closes fig, returns absolute path."""

def format_output(plain_english: str, data_section: str = "", chart_paths: list | None = None) -> str:
    """Returns: plain_english + data_section + chart paths + DISCLAIMER. Always in that order."""
```

From src/finance_mcp/server.py (current state):
```python
from fastmcp import FastMCP
from fastmcp.exceptions import ToolError
mcp = FastMCP("Finance MCP Server")

@mcp.tool
def ping() -> str: ...

@mcp.tool
def validate_environment() -> dict: ...
```

Existing test files in tests/ (do not modify):
- test_data_validation.py, test_mcp_server.py, test_output_dir.py, test_output_format.py, test_yfinance_adapter.py
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test stub file and tools package marker</name>
  <files>tests/test_market_tools.py, src/finance_mcp/tools/__init__.py</files>
  <behavior>
    All 15 test functions exist with names matching VALIDATION.md exactly.
    Each stub raises pytest.fail("not implemented") or uses `assert False, "stub"` so pytest reports FAILED (not SKIPPED or PASSED).
    The 15 required test names are:
      test_analyze_stock_returns_output
      test_analyze_stock_saves_chart
      test_get_returns_values
      test_get_returns_chart
      test_annualized_volatility_formula
      test_volatility_chart
      test_sharpe_sign
      test_max_drawdown_nonpositive
      test_beta_calculation
      test_normalized_prices_start_at_100
      test_compare_tickers_chart
      test_correlation_uses_returns
      test_correlation_map_chart
      test_output_plain_english_first
      test_output_ends_with_disclaimer
    src/finance_mcp/tools/__init__.py is an empty file.
  </behavior>
  <action>
    1. Create src/finance_mcp/tools/__init__.py as an empty file (package marker only).

    2. Create tests/test_market_tools.py with:
       - Module docstring: "Phase 2 market analysis tool tests — MKTX-01 through MKTX-07."
       - Import block at top (all needed for later implementation):
         ```python
         import pytest
         import pandas as pd
         import numpy as np
         from unittest.mock import patch, MagicMock
         ```
       - All 15 test functions listed above, each body is:
         ```python
         pytest.fail("stub — implement when tool is ready")
         ```
       - No imports from finance_mcp.tools yet (tools don't exist yet — importing would cause ImportError).

    3. Confirm stubs run as FAILED (not errors due to import failures):
       `.venv/bin/python3 -m pytest tests/test_market_tools.py -x -q` — all 15 should show as FAILED.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_market_tools.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>15 tests collected, all FAILED (pytest.fail stubs), 0 errors, 0 skipped. tools/__init__.py exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement analyze_stock tool and update two failing tests to green</name>
  <files>src/finance_mcp/tools/price_chart.py, src/finance_mcp/server.py, tests/test_market_tools.py</files>
  <behavior>
    test_analyze_stock_returns_output: calls analyze_stock with mocked adapter; returned string starts with a non-numeric character (plain-English text) and ends with DISCLAIMER.
    test_analyze_stock_saves_chart: calls analyze_stock with real synthetic data using tmp_path fixture; a .png file is created in finance_output/charts/.
  </behavior>
  <action>
    1. Create src/finance_mcp/tools/price_chart.py:

    ```python
    """analyze_stock MCP tool — price chart for a single ticker (MKTX-01)."""
    from finance_mcp.output import save_chart, format_output  # MUST import output first (sets Agg backend)
    import matplotlib.pyplot as plt
    import pandas as pd
    from finance_mcp.adapter import fetch_price_history, get_adjusted_prices, DataFetchError
    from fastmcp.exceptions import ToolError


    def analyze_stock(ticker: str, start: str, end: str = "") -> str:
        """
        Generate a price chart for a stock ticker over the given date range.

        Args:
            ticker: Yahoo Finance ticker symbol, e.g. "AAPL"
            start: Start date in ISO format "YYYY-MM-DD"
            end: End date "YYYY-MM-DD". Defaults to today if omitted.

        Returns:
            Formatted output with plain-English price trend summary, chart path, and disclaimer.
        """
        try:
            df = fetch_price_history(ticker, start=start, end=end or None)
        except DataFetchError as exc:
            raise ToolError(str(exc)) from exc

        prices = get_adjusted_prices(df)
        ticker_upper = ticker.strip().upper()

        # Build price chart
        fig, ax = plt.subplots(figsize=(12, 6))
        ax.plot(prices.index, prices.values, linewidth=1.5, color="#1f77b4")
        ax.set_title(f"{ticker_upper} Adjusted Close Price")
        ax.set_xlabel("Date")
        ax.set_ylabel("Price (USD)")
        ax.grid(True, alpha=0.3)
        fig.tight_layout()

        end_label = end if end else "today"
        filename = f"{ticker_upper.lower()}_price_{start}_{end_label}.png"
        chart_path = save_chart(fig, filename)

        # Plain-English interpretation
        start_price = prices.iloc[0]
        end_price = prices.iloc[-1]
        pct_change = (end_price - start_price) / start_price * 100
        direction = "increased" if pct_change >= 0 else "decreased"
        interpretation = (
            f"{ticker_upper} {direction} {abs(pct_change):.1f}% from {start} to {end_label}. "
            f"Starting price: ${start_price:.2f}. Ending price: ${end_price:.2f}. "
            f"The price chart has been saved for visual inspection."
        )

        return format_output(
            plain_english=interpretation,
            chart_paths=[chart_path],
        )
    ```

    2. Update server.py — add these lines AFTER the existing imports and BEFORE `if __name__ == "__main__":`:

    ```python
    from finance_mcp.tools.price_chart import analyze_stock

    mcp.add_tool(analyze_stock)
    ```

    Do NOT use the @mcp.tool decorator on imported functions — use mcp.add_tool() for all Phase 2 tools.

    3. Update tests/test_market_tools.py — replace the two stub bodies for test_analyze_stock_returns_output and test_analyze_stock_saves_chart:

    ```python
    def test_analyze_stock_returns_output():
        """analyze_stock returns formatted string starting with plain English, ending with DISCLAIMER."""
        import pandas as pd
        from unittest.mock import patch
        from finance_mcp.output import DISCLAIMER

        # Synthetic price series: 10 trading days, starting at 100
        dates = pd.date_range("2024-01-02", periods=10, freq="B")
        mock_df = pd.DataFrame({"Close": [100, 101, 102, 103, 104, 105, 106, 107, 108, 110]}, index=dates)

        with patch("finance_mcp.tools.price_chart.fetch_price_history", return_value=mock_df):
            from finance_mcp.tools.price_chart import analyze_stock
            result = analyze_stock("AAPL", "2024-01-02", "2024-01-15")

        assert isinstance(result, str)
        # Must start with plain-English text (letter, not digit or special)
        assert result[0].isalpha(), f"Output must start with plain-English text, got: {result[:40]!r}"
        assert result.strip().endswith(DISCLAIMER)


    def test_analyze_stock_saves_chart(tmp_path, monkeypatch):
        """analyze_stock saves a PNG to finance_output/charts/."""
        import pandas as pd
        from unittest.mock import patch
        import os

        # Redirect chart output to tmp_path
        monkeypatch.chdir(tmp_path)

        dates = pd.date_range("2024-01-02", periods=10, freq="B")
        mock_df = pd.DataFrame({"Close": [100, 101, 102, 103, 104, 105, 106, 107, 108, 110]}, index=dates)

        with patch("finance_mcp.tools.price_chart.fetch_price_history", return_value=mock_df):
            from finance_mcp.tools.price_chart import analyze_stock
            result = analyze_stock("AAPL", "2024-01-02", "2024-01-15")

        chart_dir = tmp_path / "finance_output" / "charts"
        png_files = list(chart_dir.glob("*.png"))
        assert len(png_files) == 1, f"Expected 1 PNG in {chart_dir}, found {png_files}"
    ```

    Keep the remaining 13 stubs as-is (pytest.fail).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_market_tools.py::test_analyze_stock_returns_output tests/test_market_tools.py::test_analyze_stock_saves_chart -v 2>&1 | tail -10</automated>
  </verify>
  <done>2 tests PASSED. 13 remaining stubs still FAILED. server.py imports and registers analyze_stock without errors: `.venv/bin/python3 -c "from finance_mcp.server import mcp; print('OK')"` prints OK.</done>
</task>

</tasks>

<verification>
After this plan:
- `.venv/bin/python3 -m pytest tests/test_market_tools.py -q` — 2 PASSED, 13 FAILED (stubs), 0 errors.
- `.venv/bin/python3 -c "from finance_mcp.tools.price_chart import analyze_stock; print(analyze_stock.__doc__[:30])"` — prints docstring.
- `.venv/bin/python3 -c "from finance_mcp.server import mcp"` — no ImportError.
- `.venv/bin/python3 -m pytest tests/ -q` — existing tests still green, 0 regressions.
</verification>

<success_criteria>
- tests/test_market_tools.py has 15 test functions with exact names from VALIDATION.md
- tools/__init__.py exists and is importable as a package
- tools/price_chart.py exports analyze_stock with full docstring and ToolError handling
- server.py has mcp.add_tool(analyze_stock) without breaking existing ping/validate_environment tools
- 2 analyze_stock tests pass; 13 stubs remain as FAILED (not errors)
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-analysis-tools/02-01-SUMMARY.md`
</output>
