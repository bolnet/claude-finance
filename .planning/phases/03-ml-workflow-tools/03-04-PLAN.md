---
phase: 03-ml-workflow-tools
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - tests/test_ml_tools.py
autonomous: true
requirements:
  - LQDX-04
  - LQDX-05
  - LQDX-06
  - INVX-01
  - INVX-02
  - INVX-03
  - INVX-04
  - INVX-05
  - INVX-06

must_haves:
  truths:
    - "All 14 tests in tests/test_ml_tools.py pass — no skips remaining on implemented functionality"
    - "Full test suite (tests/) is green — no Phase 2 regressions"
    - "server.py has 5 new ML tool registrations: ingest_csv, liquidity_predictor, predict_liquidity, investor_classifier, classify_investor"
    - "finance_output/models/ directory contains both persisted pipelines after test run"
  artifacts:
    - path: "tests/test_ml_tools.py"
      provides: "All 14 tests passing (no skips on tool stubs)"
    - path: "finance_output/models/liquidity_pipeline.joblib"
      provides: "Persisted liquidity regression pipeline"
    - path: "finance_output/models/investor_pipeline.joblib"
      provides: "Persisted investor classifier pipeline"
  key_links:
    - from: "tests/test_ml_tools.py"
      to: "src/finance_mcp/tools/liquidity_predictor.py"
      via: "direct import of liquidity_predictor and predict_liquidity"
      pattern: "from finance_mcp.tools.liquidity_predictor import"
    - from: "tests/test_ml_tools.py"
      to: "src/finance_mcp/tools/investor_classifier.py"
      via: "direct import of investor_classifier and classify_investor"
      pattern: "from finance_mcp.tools.investor_classifier import"
---

<objective>
Integration wave: remove all test skips from test_ml_tools.py, verify all 14 tests pass end-to-end, and confirm the full test suite is green with all 5 ML tools registered.

Purpose: 03-02 and 03-03 run in parallel (Wave 2). This Wave 3 plan is the integration point where both branches merge. Any import guards or pytest.skip() placeholders added in 03-01 must now be replaced with real imports and full assertions.

Output: All 14 ML tests green, full suite green, SKILL.md updated to document ML tools.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-ml-workflow-tools/03-RESEARCH.md
@src/finance_mcp/server.py
@.planning/phases/03-ml-workflow-tools/03-02-SUMMARY.md
@.planning/phases/03-ml-workflow-tools/03-03-SUMMARY.md

<interfaces>
<!-- Tools now exist — executor needs the signatures to write full test assertions. -->

From src/finance_mcp/tools/liquidity_predictor.py (created in 03-02):
```python
def liquidity_predictor(csv_path: str, target_column: str = "liquidity_risk") -> str:
    """Returns formatted string with RMSE, R², residual chart path, ends with DISCLAIMER."""

def predict_liquidity(credit_score: float, debt_ratio: float, region: str, model_path: str = "") -> str:
    """Returns formatted string with predicted liquidity risk value and context, ends with DISCLAIMER."""
```

From src/finance_mcp/tools/investor_classifier.py (created in 03-03):
```python
def investor_classifier(csv_path: str, target_column: str = "segment") -> str:
    """Returns formatted string with confusion matrix path, classification report, feature importance path, ends with DISCLAIMER."""

def classify_investor(age: float, income: float, risk_tolerance: float, product_preference: str, model_path: str = "") -> str:
    """Returns formatted string with segment label, confidence, feature explanation, ends with DISCLAIMER."""
```

From src/finance_mcp/tools/csv_ingest.py (created in 03-01):
```python
def ingest_csv(csv_path: str, target_column: str = "") -> str:
    """Returns formatted string with structure summary, EDA stats, chart paths, ends with DISCLAIMER."""
```

Fixture CSV paths (absolute safe for test use):
- "tests/data/liquidity_sample.csv"
- "tests/data/liquidity_client_sample.csv"
- "tests/data/investor_sample.csv"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate all test stubs and verify full ML test suite passes</name>
  <files>
    tests/test_ml_tools.py
  </files>
  <action>
    Open tests/test_ml_tools.py. For every test that currently has a pytest.skip() or pytest.importorskip() placeholder protecting a liquidity_predictor, predict_liquidity, investor_classifier, or classify_investor import — replace with a direct import and a full assertion body.

    For each previously-skipped test, implement the full test body matching the behavior blocks from 03-01 Task 1. Specifically:

    test_split_before_fit: Import liquidity_predictor. Call liquidity_predictor("tests/data/liquidity_sample.csv"). Assert the returned string contains "RMSE" and is a str. (The split-before-fit enforcement is in the tool's code structure — this test validates the tool runs without error and produces evaluation output.)

    test_regression_evaluation: Call liquidity_predictor("tests/data/liquidity_sample.csv"). Assert "RMSE" in result, "R²" in result or "R2" in result, result ends with DISCLAIMER text.

    test_predict_liquidity: First call liquidity_predictor("tests/data/liquidity_sample.csv") to train/persist model. Then call predict_liquidity(credit_score=650.0, debt_ratio=0.4, region="North"). Assert result is a str and contains a digit (the numeric prediction value). Assert result ends with DISCLAIMER text.

    test_investor_csv_detection: Call ingest_csv("tests/data/investor_sample.csv"). Assert "segment" in result (or whatever target column name is in the fixture).

    test_feature_engineering: Import _detect_structure, _clean_dataframe from csv_ingest. Create small DataFrame with one categorical column. Apply pd.get_dummies(df, drop_first=True). Assert result dtypes include bool. Assert the original categorical column name is NOT in result.columns.

    test_stratified_split: Load investor_sample.csv. Apply get_dummies. Call train_test_split(X, y, test_size=0.2, stratify=y, random_state=42). Compute class proportions in y_train and y_test. Assert that for each class, |prop_train - prop_test| < 0.10.

    test_gridsearch_runs: Call investor_classifier("tests/data/investor_sample.csv"). Assert "best parameters" in result.lower() or "best_params" in result.

    test_classifier_evaluation: Call investor_classifier("tests/data/investor_sample.csv"). Assert "confusion matrix" in result.lower(). Assert "precision" in result.lower(). Assert "recall" in result.lower(). Assert at least one ".png" path in result.

    test_classify_investor: First call investor_classifier("tests/data/investor_sample.csv") to persist model. Then call classify_investor(age=35.0, income=75000.0, risk_tolerance=0.6, product_preference="stocks"). Assert result is a str. Assert one of ["conservative", "moderate", "aggressive"] is in result.lower().

    test_ml_output_format: For each tool (ingest_csv, liquidity_predictor, investor_classifier), assert returned string does NOT start with whitespace-only content and DOES end with the DISCLAIMER string. Import DISCLAIMER from finance_mcp.output.

    test_missing_csv_error: from fastmcp.exceptions import ToolError. Use pytest.raises(ToolError): call ingest_csv("/nonexistent/path/to/file.csv").

    Run the full ML test suite to confirm all 14 pass:
    `.venv/bin/python3 -m pytest tests/test_ml_tools.py -v`

    If any test fails, investigate and fix. Common issues:
    - Column name mismatch between fixture CSV and tool default: check the fixture CSV headers match target_column defaults
    - DISCLAIMER exact-match: use `result.endswith(DISCLAIMER)` or `DISCLAIMER in result` — format_output ends with DISCLAIMER but may have trailing newlines; use `DISCLAIMER in result`
    - Model persistence path: if tests run in different working directories, use absolute paths for MODEL_PATH in the tools

    After all 14 pass, run the full test suite:
    `.venv/bin/python3 -m pytest tests/ -q`
    Confirm all Phase 2 tests still pass.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_ml_tools.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 14 tests in test_ml_tools.py show PASSED. Full suite shows no failures.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md to document ML tool routing</name>
  <files>
    .claude/skills/finance/SKILL.md
  </files>
  <action>
    Read the existing SKILL.md at .claude/skills/finance/SKILL.md. Locate the MCP tools section (the section listing available tools Claude should route requests to). Add entries for the 5 new Phase 3 ML tools following the same format as the existing Phase 2 tool entries.

    Add under a new "ML Workflow Tools" subsection:

    - **ingest_csv(csv_path, target_column?)** — Use when: user wants to explore a CSV, detect columns, or get summary stats before modeling. Keywords: "explore csv", "what's in this file", "data summary", "clean my data"
    - **liquidity_predictor(csv_path, target_column?)** — Use when: user wants to build a liquidity risk model. Keywords: "predict liquidity", "liquidity model", "train regression", "liquidity risk from CSV"
    - **predict_liquidity(credit_score, debt_ratio, region)** — Use when: user has a trained liquidity model and wants to score a new client. Keywords: "predict for client", "score this customer", "liquidity score for"
    - **investor_classifier(csv_path, target_column?)** — Use when: user wants to classify investors into segments. Keywords: "classify investors", "investor segments", "train classifier", "segment from CSV"
    - **classify_investor(age, income, risk_tolerance, product_preference)** — Use when: user wants to classify a single new investor. Keywords: "what segment is this client", "classify investor", "investor type"

    Also add a note: "ML tools require a CSV file path the user provides at runtime. Course CSV files (liquidity_data.csv, investor_data.csv) must be placed at the path the user specifies — they are not bundled with the skill."

    Do not remove or modify any existing Phase 2 content in SKILL.md.
  </action>
  <verify>
    <automated>.venv/bin/python3 -c "
content = open('.claude/skills/finance/SKILL.md').read()
assert 'ingest_csv' in content, 'ingest_csv not in SKILL.md'
assert 'liquidity_predictor' in content, 'liquidity_predictor not in SKILL.md'
assert 'investor_classifier' in content, 'investor_classifier not in SKILL.md'
print('SKILL.md updated correctly')
"</automated>
  </verify>
  <done>SKILL.md contains entries for all 5 ML tools with routing keywords. Existing Phase 2 content unchanged.</done>
</task>

</tasks>

<verification>
All 14 ML tests pass:
`.venv/bin/python3 -m pytest tests/test_ml_tools.py -q`

Full suite green:
`.venv/bin/python3 -m pytest tests/ -q`

Server registers all 5 ML tools:
`.venv/bin/python3 -c "
from finance_mcp.server import mcp
tool_names = [t.name for t in mcp.list_tools() if hasattr(mcp, 'list_tools')] or 'inspect manually'
print('Server imports OK')
"`
</verification>

<success_criteria>
- All 14 tests in tests/test_ml_tools.py pass with no skips on implemented functionality
- Full test suite (tests/) passes with no regressions
- SKILL.md documents all 5 new ML tools with routing keywords
- Both model joblib files exist in finance_output/models/ after test run
</success_criteria>

<output>
After completion, create `.planning/phases/03-ml-workflow-tools/03-04-SUMMARY.md`
</output>
