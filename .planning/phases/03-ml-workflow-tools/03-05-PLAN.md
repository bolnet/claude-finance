---
phase: 03-ml-workflow-tools
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified: []
autonomous: false
requirements:
  - LQDX-01
  - LQDX-02
  - LQDX-03
  - LQDX-04
  - LQDX-05
  - LQDX-06
  - INVX-01
  - INVX-02
  - INVX-03
  - INVX-04
  - INVX-05
  - INVX-06

must_haves:
  truths:
    - "User can call ingest_csv on a real course CSV and receive structure summary and EDA charts"
    - "User can call liquidity_predictor on liquidity_data.csv and receive RMSE, R², and residual plot"
    - "User can call predict_liquidity with a new client row and receive a numeric risk prediction"
    - "User can call investor_classifier on investor_data.csv and receive confusion matrix, classification report, and feature importance"
    - "User can call classify_investor with a new investor row and receive a segment label"
    - "All outputs lead with plain-English and end with DISCLAIMER"
    - "Output charts are visually meaningful (residual plot centered near zero, confusion matrix readable, feature importance shows real column names)"
  artifacts:
    - path: "finance_output/charts/"
      provides: "PNG charts from real CSV run: eda_*.png, residual_plot.png, confusion_matrix.png, feature_importance.png"
    - path: "finance_output/models/"
      provides: "Both persisted pipelines: liquidity_pipeline.joblib, investor_pipeline.joblib"
  key_links:
    - from: "User-provided liquidity_data.csv"
      to: "liquidity_predictor MCP tool"
      via: "csv_path parameter pointing to user's file"
      pattern: "liquidity_data\\.csv"
    - from: "User-provided investor_data.csv"
      to: "investor_classifier MCP tool"
      via: "csv_path parameter pointing to user's file"
      pattern: "investor_data\\.csv"
---

<objective>
Phase 3 functional verification: test all five ML tools end-to-end using the real course CSV files (liquidity_data.csv, investor_data.csv) that the user provides at runtime.

Purpose: Unit tests in plans 03-01 through 03-04 use synthetic fixtures. This checkpoint verifies the tools work on real data with unknown column schemas, real distributions, and the actual ML 03 and ML 05-06 course datasets. Human sign-off confirms charts are visually meaningful and plain-English interpretations make finance sense.

Output: Human approval that all ML tools work end-to-end with real data.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/finance_mcp/server.py
@.planning/phases/03-ml-workflow-tools/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated full-suite verification</name>
  <files></files>
  <action>
    Run the full test suite to confirm all tests green before functional testing:
    `.venv/bin/python3 -m pytest tests/ -q`

    Confirm server starts cleanly:
    `.venv/bin/python3 -c "from finance_mcp.server import mcp; print('[OK] Server imports correctly')"`

    Report test counts: how many tests total, how many pass.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite passes with 0 failures. Server imports without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Five ML tools registered in the Finance MCP Server:
    1. ingest_csv — CSV structure detection, EDA, data cleaning
    2. liquidity_predictor — regression pipeline with RMSE/R²/residual plot
    3. predict_liquidity — single-client liquidity risk prediction
    4. investor_classifier — RandomForest with GridSearchCV, confusion matrix, feature importance
    5. classify_investor — single-investor segment classification

    All tools follow the existing project conventions: import output.py first (Agg backend), format_output() for all responses, ToolError for user-facing errors, mcp.add_tool() registration.

    Unit tests pass with synthetic fixture CSVs. This checkpoint verifies the tools work on real course CSVs.
  </what-built>
  <how-to-verify>
    You will need your course CSV files: liquidity_data.csv and investor_data.csv (from the ML 03 and ML 05-06 curriculum).

    STEP 1 — Liquidity workflow:
    Ask Claude: "Use the ingest_csv tool on my liquidity_data.csv file at [your path]"
    Expected: Plain-English summary of column types, missing values, outlier count. At least one eda_*.png chart path in output.

    STEP 2 — Train liquidity model:
    Ask Claude: "Use liquidity_predictor on [your liquidity_data.csv path]"
    Expected: Output contains RMSE value, R² value, baseline comparison, residual plot path. Plain-English interpretation explains what the numbers mean. Ends with disclaimer.

    STEP 3 — Open residual plot:
    Open finance_output/charts/residual_plot.png.
    Check: residuals scattered roughly symmetrically around zero (not obviously skewed or patterned). Histogram roughly bell-shaped.

    STEP 4 — Predict for new client:
    Ask Claude: "Use predict_liquidity with credit_score=700, debt_ratio=0.35, region=North" (or use values relevant to your dataset's columns)
    Expected: Numeric prediction value with confidence context. Ends with disclaimer.

    STEP 5 — Investor workflow:
    Ask Claude: "Use investor_classifier on [your investor_data.csv path]"
    Expected: "best parameters" in output, confusion matrix PNG path, classification report with precision/recall/F1, feature importance PNG path. Ends with disclaimer.

    STEP 6 — Open confusion matrix:
    Open finance_output/charts/confusion_matrix.png.
    Check: matrix is readable, class labels match your CSV's segment values.

    STEP 7 — Open feature importance:
    Open finance_output/charts/feature_importance.png.
    Check: bar chart labels are real column names from your CSV (not generic indices).

    STEP 8 — Classify a new investor:
    Ask Claude: "Use classify_investor with age=42, income=95000, risk_tolerance=0.7, product_preference=stocks" (or relevant values)
    Expected: Segment label (e.g. "aggressive"), confidence percentage, top 3 feature names with importance scores. Ends with disclaimer.

    PASS criteria:
    - All 5 tools return output (no ToolError on valid inputs)
    - Residual plot is visually sensible
    - Confusion matrix labels match real class names
    - Feature importance bars show real column names
    - Plain-English interpretations make finance sense (not gibberish)
    - All outputs end with the disclaimer text

    FAIL criteria (report these):
    - Any tool raises an error on real CSV data
    - Charts are blank, corrupted, or show index numbers instead of column names
    - Output does not end with disclaimer
    - RMSE or accuracy values appear nonsensical for the domain
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific failures so they can be diagnosed and fixed.</resume-signal>
</task>

</tasks>

<verification>
Automated:
`.venv/bin/python3 -m pytest tests/ -q`

Manual (in checkpoint above):
- ingest_csv on real liquidity CSV returns structure summary
- liquidity_predictor returns RMSE + R² + residual plot
- predict_liquidity returns numeric prediction
- investor_classifier returns confusion matrix + classification report + feature importance
- classify_investor returns segment label with explanation
- All charts visually meaningful
- All outputs end with DISCLAIMER
</verification>

<success_criteria>
- Full automated test suite green (0 failures)
- Human signs off that all 5 tools work end-to-end on real course CSV files
- Residual plot, confusion matrix, and feature importance charts pass visual inspection
- Phase 3 complete — ready for `/gsd:verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/03-ml-workflow-tools/03-05-SUMMARY.md`
</output>
